# SOP 合规检测系统 — 方案与开发路线

日期：2026-08-30
基座：[`NVIDIA/sop-monitoring-blueprints`](https://github.com/NVIDIA/sop-monitoring-blueprints) —— **仓库固定，版本不固定**（跟随 NVIDIA 仓库更新）。本文各项实测结论均标注复核时所在提交；NVIDIA 仓库更新后须重跑契约测试（§5.9）确认前提未变。

本文事实的复核基准：`6e149568`（初次实测）与 `69352021`（当前 main，复核）。两者 `sop-inference-bp` **零差异**，故推理服务相关结论在两个提交上等价成立。

本文三类结论分别标注：**【已定】**= 需求方已确认；**【实测】**= 本机跑过、有数据；**【待确认】**= 我的建议，需你裁决。

---

## 一、产品定位与硬约束

把工厂标准作业转成机器可执行的模板，在现场实时回答：当前在做什么、是否合规、偏离后留什么证据、触发什么响应。

**【已定】基座是本产品的躯干，不是外部依赖。** NVIDIA 仓库已实现的能力原样复用；接近但不够的就地改造；基座完全没有的才新建。不允许出现"基座有一套、我们再写一套"的重复实现——这条约束优先于"`vendor/` 只读"这类洁癖，见 §5.11 与 [ADR-0007](../adr/0007-base-is-the-trunk-not-a-dependency.md)。

**【已定】交付的必须是真实可用的系统，不允许任何 mock / 模拟实现。首要价值是实时防错。**

**【已定】推理机是自治判定单元；中心不可达时现场继续工作。** 每台推理机独立完成取流、感知、判定、违规锁存、处置（含写输出点位）与证据缓冲，本地持有模板版本、设备配置与凭据。中心是管理面与聚合面，不在实时判定路径上。中心离线时推理机按最后确认的模板版本继续判定与处置，本地排队，恢复后对账；处置不重复执行，证据不丢失。见 [ADR-0005](../adr/0005-judgment-runs-inside-the-inference-host.md)。

**【已定】性能优先。每个成功实时结果的端到端延迟目标为 ≤ 500 ms。** 该目标按结果类型分两个起点（§5.6）：**到达即判类**（错步、反序、超时、顺序型模板的跳号漏步）起点是形成该结果所需的最后一帧在相机侧产生；**闭合类**（实例闭合时的集合比对）起点是闭合条件成立时刻。终点同为对应状态或判定完成 Web 渲染。目标环境门禁要求测试窗口内超预算结果为零，同时报告 p50/p95/p99、最大值、超预算计数、队列深度与积压趋势；未在目标 GPU、相机、网络和浏览器上通过前，不把该目标表述为已实现能力。

**【已定】优先对齐推理服务公开契约，不为传输层另造体系。** 视频帧留在推理机；DeepStream/PyServiceMaker 形成 chunk，经 VLM 分类后由本机 supervisor 消费 SSE 判定，再由本机上报中心。REST/JSON 负责配置、查询和命令；中心向 Web 推送 SSE。关键路径不增加 Kafka、gRPC、自定义 WebSocket 或数据库轮询，除非 profiling 证明必要。

---

## 二、实测事实（方案的地基）

### 2.1 基座判定层分不清"没做"与"没看到"

基座 `nvds_action_detector` 的 `__init__.py`、`ds_logger.py`、`missing_number_detector.py`、`sop_step_checker.py` **只依赖标准库**，本机纯 CPU 可直接运行。同一份 5 步 `actions.json`，输入动作序列 `[1,2,4,5]`：

| 场景 | 基座输出 |
|---|---|
| 真漏步（操作员跳过第 3 步） | `missing=[3]` |
| 断流（第 3 步期间视频中断） | `missing=[3]` |

**输出完全相同**。基座无任何通道区分二者，且 checker 请求里根本没有流健康字段 → 三值判定（通过 / 不通过 / 不可判定）必须由我们自己实现（§5.2），并需要一条把流健康送出推理服务的通道（§5.11）。

### 2.2 基座的周期边界启发式会把合规作业误判为违规

这是本方案最关键的一条实测。5 步 SOP，操作员做 1,2,3 后**返工重做 2**，再继续 4,5——一遍完全合规的作业：

```
输入(1)  missing=[]        输入(2)  missing=[]        输入(3)  missing=[]
输入(2)  missing=[4,5]  ← 返工触发周期边界，误报漏步
输入(4)  missing=[]        输入(5)  missing=[]
终帧     final_missing=[1,3]  ← 再误报一次
```

一遍合规作业被判了两次不合规。根因在 `missing_number_detector.py:149-166`：见重复号且已见 `N×0.6` 个动作即认定新周期开始，`seen_in_cycle` 被清空；或低号 ≤`N×0.3` 出现在高号 ≥`N×0.8` 之后同样触发。返工在工厂是常态，该启发式在产线上不可用。

→ **`MissingNumberDetector` 不是状态机，是靠两条启发式猜周期边界的有状态序列校验器。** 序列比对逻辑可用且应复用；**周期边界必须换成模板声明的信号**（§5.1、[ADR-0006](../adr/0006-cycle-boundary-is-declared-not-inferred.md)）。

### 2.3 基座对漏步的报出时机达不到实时

逐 chunk 模拟真实流式（漏第 3 步的一遍作业）：

```
chunk1 动作(1)  cycle_completed=False  missing=[]
chunk2 动作(2)  cycle_completed=False  missing=[]
chunk3 动作(4)  cycle_completed=False  missing=[]
chunk4 动作(5)  cycle_completed=False  missing=[]
```

作业已结束，基座完全沉默。根因在 `process_number`：`cycle_completed=True` 的条件是 `len(seen_in_cycle) == N`，漏一步永远凑不满。`missing` 只在两个时机产出：

1. 下一遍作业开始时触发周期边界（实测 `[1,2,4,5,1,2,3,4,5]` → 第二遍开始才吐 `missing=[3]`）；
2. 流结束（`keep_alive=False`，SSE 终帧）→ 吐 `final_missing=[3]`。

对 7×24 常驻流，时机 2 永不发生 → **当班最后一遍的漏步永远不报**。声明式边界 + 空闲时限在源头消除这个缺陷（§5.1）。

### 2.4 流健康信号存在于 pipeline，但被丢弃

复核 `69352021` 后修正早期"无重连"结论：RTSP pipeline 给 `nvurisrcbin` 设置了 `init-rtsp-reconnect-interval=10`（`ds_3d_action_pipeline.py:491`）。按 NVIDIA DeepStream 源码，该属性在 RTSP 源收到错误时等待后触发重连；基座没有设置用于"持续无数据"检测的 `rtsp-reconnect-interval`，也没有显式设置重连次数。

关键在于 `ds_boundary_infernce` 的 `on_message` 回调（`ds_3d_action_pipeline.py:779-784`）**只处理 EOS**：source error、正在重连、重连成功、最后一帧时刻、恢复后时间轴归零全部被丢弃，从未进入 SSE。`checker_result.error_message` 也只反映 checker 自身异常。

→ 流健康的第一手信号就在推理机的 pipeline 回调里。**判定必须与它同机**，否则中心侧只能靠独立探活二次猜测——那正是"两套东西"。改造方案见 §5.7。完整证据见 [`nvidia-base-capability-boundary.md`](../research/nvidia-base-capability-boundary.md)。

### 2.5 基座三项能力空白

- **无时间概念**：`deadlineMs`、超时、时限一概不存在，`time.time()` 只测性能。超时违规须由我们从 chunk 的 `start_time`/`end_time` 与主机单调钟自己算。
- **无处置动作**：对外响应面只有 Kafka 发消息（`messager.py`）与 `playsound` 播告警音。
- **无 ROI / 外部信号 / 目标检测。**

### 2.6 检查器阈值在部署态不可配（实测）

`ChatCompletionRequest`（`api_types.py:245`）**没有** SOP 检查阈值字段；`ds_sop_process.py:675` 硬编码 `0.6/0.3/0.8`，:735 从 chunk dict 读同名键，而**全仓无任何代码往 chunk 写这三个键**（已 grep 确认）。`SopDetectionOptions` 只用于内部 checker 服务，不在公开契约上。

→ 部署态阈值恒为默认值，且无环境变量可改。这三个阈值只服务于将被替换掉的边界启发式（§2.2），换成声明式边界后它们不再有意义。模板版本的运行参数只含产品侧可调参数的**默认值**（空闲时限、超时时限、处置策略），生效值的权威见 §5.3；**顺序性声明不是运行参数**。

### 2.7 DDM 权重不随仓库提供，但可自训

`triton_model_repo/ddm/1/model.py` 里写死：

```
"DDM checkpoint not found at {...}. A fresh box has no checkpoint until you
fine-tune/download one. Obtain a DDM-Net checkpoint (e.g. via the SOP Training
Blueprint), place it under MODEL_ROOT_DIR, and set DDM_MODEL_PATH..."
```

`api_server.main()` 在非 DUMMY 模式下调 `wait_for_model_ready()` → **没有 DDM 权重，服务起不来**。

训练程序支持单 GPU 配置：`ddm_train_config.yaml` 为 `num_gpus: 1`、`resnet50`、`resolution: 224`、`frames_per_side: 5`、`batch_size: 4`、`epochs: 10`、`amp: false`。这不等于已证明单张 4090 的峰值显存、训练时长或准确率；须按目标环境验证矩阵实测。

**DDM 标注格式**（实测 `ddm_dataset.py:330-395`）：

```json
{ "<video_id>": [ {"start_timestamp": 0.0, "end_timestamp": 6.67, "description": "..."}, ... ] }
```

整段视频需在 `data_root/{video_id}.mp4`；边界 = 相邻片段 `end_timestamp` 与 `start_timestamp` 的中点 × fps；描述含 "final segment" 的片段被过滤。

**本地样本数据可转换**（实测）：`server_fan/raw/` 有 12 个整段视频，`server_fan/train/Install_N/` 是切好的片段。以 Install_8 为例：raw 视频时长 71.54s，11 个片段时长合计 71.54s，差值 **0.00s（0.0%）**——片段是整段视频的完整无重叠切分。

**⚠️ 实现期必须实测的坑**：文件名格式 `{序号}_{视频}_{周期}_{动作号}.mp4`，序号 `10` 出现两次（`10_Install_8_1_1.mp4` 与 `10_Install_8_2_11.mp4`，后者周期=2）→ **按文件名排序 ≠ 时间顺序**。定序难题移交给标注环节的人（§5.4）。

样本数据自带的 `train/actions.json` 与 `augmented/{gqas,bcq,mcq,golden_gqa}` 是 **VLM 微调格式**，不是 DDM 格式。

### 2.8 视频链路成本（本机 10 核实测）

| 操作 | 单路 | 8 路并发 |
|---|---|---|
| CPU 解码（1280×592/29fps） | 0.42s / 23.24s 视频 = **55× 实时** | 2.58s / 186s = **72× 实时** |
| CPU 解码 + 缩放 224 | 37× 实时 | — |
| NVDEC 硬解（GTX 960） | 1.48s = 16× 实时 | — |
| **x264enc 转码**（1080p，推理服务 `SW_ENCODER` 确切参数 `superfast zerolatency bframes=0`） | 3.92s / 20s = **5.1× 实时**，占 ~0.2 核 | 14.96s / 160s，跟得上但吃 **~10 核** |
| **零转码 passthrough** | 0.17s = **121× 实时** | 0.21s = **773× 总吞吐** |

→ 解码取流不是瓶颈；**转码才是**。8 路软编吃 10 核，50 路需 ~60 核，不可行。

### 2.9 推理服务预览/录像路径都碰 GPU

- RTSP 输出分支：`SW_ENCODER=true`（compose 默认）走 `x264enc`（CPU 编码），`false` 走 `nvv4l2h264enc`（GPU NVENC）；两者均 `bframes=0`。但**前置必经 `nvvideoconvert`（带 `gpu-id`）**，占 GPU 显存与转换算力；且与判定支路共享 pipeline，有反压风险。
- `ENCODE_VIDEO`：对**每个 chunk 无条件编码**，无违规过滤；`encode_video_gst` 内部**写死 `nvv4l2h264enc`**，`SW_ENCODER` 管不到 → 7×24 持续 GPU 编码 + 磁盘无上限增长。

→ 两条都不能用于生产预览/证据。预览与录像走独立的 mediamtx 路径（§5.5）。

### 2.10 基座其他既定事实

- **API**：OpenAI 兼容，11 端点（`docs/openapi.json` 随仓库提交）：`POST /v1/chat/completions`（SSE，`chunk_metadata` 携带 checker 结果）、`/v1/files` CRUD、`/v1/models`、`/v1/metadata`、`/v1/live`、`/v1/startup`、`/v1/ready`、`/v1/metrics`。输入三形态：`video_url`、`input_video`(file_id)、`input_camera`(Basler)。分段：`ddm-net` 或 `uniform`（后者绕过 DDM）。
- **chunk 可用字段**：`chunk_idx`、`start_time`、`end_time`、`cv_boundary_score`、`cv_execute_time`、`first_timestamp`、`pipeline_starting_timestamp`、`pipeline_cv_ready_timestamp`、`checker_result`。
- **模板与 prompt 是进程级配置**（实测）：`ACTION_CONFIG_PATH` 在 `ds_sop_process.py:113` 于模块加载时读入，每个 `SOPVideoProcessor._initialize_checker()`（:662）从同一路径读同一份文件；API 请求无模板 id 字段。→ **一个推理服务进程只承载一份 SOP 模板**，这直接决定部署形态（§5.10）。
- **镜像**：`nvcr.io/nvidia/blueprint/vss-engine:2.4.1`（NGC 门禁）+ `deepstream:9.0-triton-multiarch` + 商业 Basler Pylon SDK。容器内 Python 版本由 DeepStream 9.0 基础镜像决定，不由我们选择（§5.11 的依赖约束由此而来）。
- **安全**：官方明写无 AuthN/AuthZ、明文 HTTP、单租户、无速率/并发限制。
- **训练侧**：5 个微服务（标注 8100、增强 5487、CR 微调 32080、DDM 微调 32100、评估 32090）+ **自带 `metadata_db`（Postgres）与 Adminer 8080**；Cosmos 全量微调官方参考 `4 × A100 80GB`。
- **agentic/**：三个 Claude 插件包（`ds-sop-skills`、`sop-agentic-ft` 6 个 skill、`vss-sop-skills`），随 subtree 进仓库即可用。

### 2.11 mediamtx 选型依据

不做重编码（瓶颈在带宽非 CPU）；支持 `sourceOnDemand` / `runOnDemand`（仅在有客户端请求时拉源流）；内置分段录像 + 保留策略 + 回放服务 + 快照提取；**WebRTC 不支持 B 帧**。

海康子码流是否无 B 帧**未经查证**（详细编码规格需登录官方文档），只查到"低码率、延迟稳定"。此项列为拿到真机首验项（§5.5），不作为已知事实使用。

### 2.12 piko `authoring_worker` 是空壳

三个文件共 130 字节，`__main__.py` 导入的子模块不存在 → `ModuleNotFoundError`，README 声称的 `--check` 跑不起来。无可复用代码。

---

## 三、已确认决策清单

| # | 决策 | 内容 |
|---|---|---|
| Q1 | 判定挂载点 | **SOP 实例**（工位上一次有明确起止的作业周期）；基座 cycle 启发式被声明式边界取代（§5.1） |
| Q2 | 感知路径 | 官方 **DDM + vLLM**，**不改感知计算路径**；改造只涉及边界、有效性与处置（§5.11） |
| Q3 | 建模板初始输入 | 仅平台 **Excel 工作簿**（工位表 + 步骤表）；原文件保留追溯，导入后的模板草稿是可编辑权威记录 |
| Q4 | 发布治理 | **模板草稿（可编辑）→ 模板版本（不可变）两态**；不设待审核与已退役状态；版本 + 工位绑定 + 原子切换 + 回滚（重新绑定历史版本）；**不做签名**；保留 sha256 完整性校验 |
| Q5/Q14 | 权限 | 本地账户 + 自定义权限；角色是权限集合、用户可有多个角色；首版角色见 §5.4 |
| Q6 | 路数 | 1~50 可配，超载由人工加硬件解决；工位与相机是两张表 |
| Q7 | 基座接入 | `git subtree` 接入（`--squash`），**固定仓库不固定版本**；**基座是躯干**：一处纯加输出的就地改造 + 序列比对自己实现 + 基座 checker/处置按配置关闭（§5.11），补丁维护为可重放 diff；更新后必须跑基座契约测试（§5.9） |
| Q8 | 流健康 | **在推理机 pipeline 回调内取得第一手信号**（§2.4），不在中心做二次探活 |
| Q9 | 处置动作 | 记录 + 前端告警 + 声光 + 暂停工位判定 + 终止实例 + **写输出点位**（物理控制，经连接器）；全部在推理机本地执行，不经中心 |
| Q10 | 后台范围 | 实时看板、告警列表、证据回看、人工复核、设备管理、模板管理、用户与权限；统计报表与整改闭环后置，保留扩展能力 |
| Q11 | 准入 | 软件不做过载准入；超载表现为该路**不可判定 + `CHUNK_BACKLOG_EXCEEDED`**，界面必须显示，不静默降级 |
| Q12 | 预览 | 浏览器直连推理机 mediamtx，优先零转码；不经过中心统一访问入口，不与判定 pipeline 耦合 |
| Q15 | 实例边界 | 见 §5.1（声明式边界替换基座启发式） |
| Q16/Q19 | 连接器 | 输入 + 输出双向；**连接器是可配置项，非所有工位都需要**；判定逻辑不因有无连接器而改变代码路径；连接器运行时在推理机内（§5.8） |
| Q17 | 多机拓扑 | 每路相机绑定到指定推理机；一台推理机可运行多个推理后端进程，**每个后端承载一份模板**（§5.10）；改绑须经交权握手，强制改绑需二人确认（§5.17） |
| Q18 | 发布权限 | 模板模块内分 `view`/`edit`/`delete`，**发布归入 `edit`**。**不做双人职责分离**——流程门禁由授权与谁不授权表达，不由软件状态机强制。唯一例外是强制改绑（§5.17），因其可能同时驱动两套物理执行器 |
| Q24 | 相机 | **海康威视网络摄像机**，RTSP 主码流 `101` / 子码流 `102` |
| Q26 | 微调策略 | **先训练 DDM，VLM 使用公开权重**；首切片只导入、校验训练数据并生成 DDM 标注，不编排训练 |
| Q27 | 多机部署形态 | 每台一份 Compose；中心生成**不含秘密**的配置骨架供下载，凭据在推理机本地录入（§5.12） |
| Q28 | 预览并发与大屏 | 视频不经中心入口或中继；浏览器直连推理机 mediamtx；首版内网依赖可信工厂网络/VLAN 与端口访问控制，不引入逐观看者 JWT |
| Q30 | 工位组织 | 首版以唯一编码、名称和可选标签组织，不预设车间/产线层级 |
| Q31 | 设备验证 | 配置可离线保存并显示"未验证"；显式"测试连接"只调用真实设备，不以失败阻止保存 |
| Q32 | 首切片 Web | 简体中文桌面 Web；Chrome/Edge 当前稳定版；支持 1366×768～1920×1080；满足键盘、标签、错误提示和非纯颜色表达等基础可访问性 |
| Q33 | 模型权威 | **推理机是模型的权威**：本机部署 vLLM、型号可自定义；中心经 `/v1/metadata`、`/v1/models` 读取该机实际加载的模型标识写入判定溯源。不建中心模型注册表，不做中心分发（§5.13） |
| Q34 | 标注与训练侧 | **原样复用基座 5 个训练微服务与 React 标注 UI**，经 Nginx 反代并在网关补鉴权；不用 Vue 重写标注界面（§5.11） |

---

## 四、待确认项

**Q25 AI 建模板做到哪一步**：(a) 只生成提示词与 `actions.json`；(b) 再加步骤对齐（调推理服务，给出每步在模板视频中的时间码供人工核对）；(c) 再加微调数据生成。
➡️ 建议 **(b)**，(c) 放阶段三。纯 (a) 太薄——客户无法在上线前判断模板可用。

**Q29 4090 排期与形态**：什么时候到？是我能直接访问的机器，还是需要我交付部署包 + 验证脚本由你执行？这决定阶段 1 的交付方式。

**Q35 训练侧数据库合并**：基座训练微服务自带 `metadata_db`（§2.10），中心后台另有一个 Postgres。
➡️ 建议**合并为一个 Postgres 实例、两个 schema**：运维一份，逻辑不纠缠。原样复用的训练服务只改连接串，不改代码。

**Q36 备份与恢复**：全仓库尚无备份/恢复要求。待定：中心 PG（含训练侧 schema）与 MinIO 的备份对象与周期；推理机本地 SQLite 是否纳入备份，及其内容可从中心重建到什么程度；恢复演练是否进发布门禁（当前 harness §6 只有 migration/rollback rehearsal）；恢复后与推理机的对账语义，避免重放已执行的处置。
➡️ 建议：中心 PG + MinIO 为备份权威；推理机 SQLite **不入备**——在飞实例按 §5.7 语义在重启后记为"运行中断"结案，已上报数据以中心为准，未上报的本地队列丢失即接受，不为它建第二套备份链路。恢复演练进发布门禁。

**Q37 审计追溯**：#5 主动放弃审计事件表，目前只有诊断日志（稳定事件名、写输出点位必产生事件并记操作人与目标点位）。若商业交付要求合规审计追溯，须重新引入并承担改造。
➡️ 待客户合规要求明确后裁决。二者留存期与防篡改要求不同，不能用诊断日志顶替审计。

**硬件门禁仍未闭合**：GPU 档位、单节点路数、VLM 实时性、海康型号/编码、ISAPI 用途、时间同步精度、离线与许可闭包的最终承诺，须待 [`target-environment-validation-matrix.md`](../research/target-environment-validation-matrix.md) 的门禁在真实环境执行并回传证据后冻结。该文件 §当前仍需用户/现场提供的信息 列出六项待登记输入；在其提供前可以完成中心后台首切片与验证工具，但不得标称对应门禁通过。

**默认按工程惯例处理，如需调整请指出**：全机 NTP 同源，推理机上报时钟偏移，超阈值该路进不可判定；进程/容器重启则在飞实例记为"运行中断"结案，不跨重启恢复（本地状态可查，但不续接为同一连续观测）；**所有保留时长与片段窗口均为可配置项，默认值见 §5.19 总则的表**——录像 7 天、观测/判定 90 天后聚合归档、证据不随录像过期删除都是那张表里的默认值，不是写死的常量。

---

## 五、关键机制设计

### 5.1 SOP 实例边界：声明式，替换基座启发式

**问题**：基座的周期边界靠两条启发式猜（§2.2），返工即误报；且漏步只在下一周期或流结束才报（§2.3）。

**方案**：保留 `MissingNumberDetector` 的序列比对逻辑，**替换其边界来源**。边界由模板声明的信号决定，不由代码分支决定，也不由动作序列形状猜。判定层只消费归一化观测，不知道某条观测来自动作识别还是某个外部连接器。

- **开实例**：模板声明的开始信号首次满足。
- **闭实例**（三者任一先到）：
  1. 模板声明的结束信号满足；
  2. 动作集合齐全（`len(seen) == N`，基座已有的 `cycle_completed` 快路径，保留）；
  3. **空闲超时**——最后一条观测到达后超过该工位生效的"空闲时限"仍无新观测。生效值的权威是工位，模板版本只提供默认值（§5.3）。
- **闭合前强制过有效性门**（§5.2）：查该路流健康与推理可达性，任一不健康则闭合为不可判定，**不得进入漏步集合比对**。

**空闲时限必须用推理机主机单调钟度量，不能用 chunk 时间戳。** chunk 时间戳无法度量"chunk 不再到达"。这一点关系到安全不变量：断流后 chunk 停止到达 → 计时器触发 → 若直接算漏步就会输出不通过，正是 §2.1 教训的复现。上一道有效性门就是为拦住这条路径而存在。

**顺序型模板的漏步提前报出**：模板声明步骤严格有序时，下一动作到达且跳号即判漏步，闭合时的集合比对退为兜底。不做这件事，"实时防错"对漏步就兑现不了——而漏步是四类违规里最常见的。

**【已定】首版不支持可跳过步骤。** 模板的每个步骤都是必做的：缺失即漏步。我们生成的 `actions.json` 不含 `actions_can_be_skipped` 字段。

理由是基座那个字段表达不了产品需要的语义。实测（契约测试已固化）：声明可跳过的动作被**从期望集合剔除**，且**其观测被整个丢弃**——5 步模板声明 3 号可跳过时，输入 `1,2,4,5` 判通过，而真做了 3 号那条观测不参与任何顺序判断。NVIDIA 自己的文档也这样用它：标注服务 README 的示例是 `"(10) doing action not belong to SOP"`，评估侧描述为"比较前从黄金序列与预测序列中移除"。所以它的含义是**"这个动作不属于本 SOP"**，不是"这一步可选"。

产品若要"可选步骤"，需要基座没有的第三态：不做不违规，但做了要计入顺序（顺序型模板中它必须落在正确位置）。该语义留待真实模板出现后再定——当前无客户 Excel 可依据，凭猜测设计字段不如不设。代价明确：首版无法表达可选步骤，也无法统计可选步骤的实际执行率。

**返工不再误报**：边界不再由重复号触发，`seen_in_cycle` 不会在实例中途被清空。§2.2 的用例固化为回归测试（§5.9）。

可声明为开始/结束信号的来源，取决于该工位配了什么：

| 信号源 | 前提 |
|---|---|
| 动作编号（VLM 输出） | 无（所有工位可用） |
| 外部信号（已配置的连接器点位，语义如"工件到位"） | 该工位配了对应连接器 |

**单通道感知的固有局限**（记入 [ADR-0009](../adr/0009-single-channel-perception-limit.md)）：仅以动作编号为开始信号时，操作员漏做**起始动作**则实例开不了，该遍作业完全不被记录（而非误判违规）。这不是实现缺陷。若某工位对此不可接受，可为其配置一个外部信号作为开始信号——"工件到位"是物理事实、不依赖动作识别。这是**部署期的配置选择**，不是产品架构前提：没配的工位走同一套代码路径，只是可声明的信号源少一个。

### 5.2 三值判定与原因码

**安全不变量（必须在代码中显式成立）**：

```
if 证据覆盖不足 or 流不健康 or 推理不健康 or 时间未对齐:
    判定结果 != 不通过
```

判定与该不变量都在推理机的判定核心内实现（[ADR-0005](../adr/0005-judgment-runs-inside-the-inference-host.md)），因为有效性所需的第一手信号在本机 pipeline 回调里（§2.4）。

不可判定原因码（10）：`STREAM_LOST`、`INFERENCE_BACKEND_UNREACHABLE`、`INFERENCE_TIMEOUT`、`TIMESTAMP_DISCONTINUITY`、`CHUNK_BACKLOG_EXCEEDED`、`ACTION_ID_UNKNOWN`（VLM 输出动作号不在模板声明内）、`INFERENCE_HOST_DOWN`、`RUN_INTERRUPTED`（配置切换中止 / 进程重启）、`IO_SIGNAL_LOST`（模板依赖的输入点位不可达）、`IO_TIME_UNALIGNED`（外部信号与视频时间轴对齐偏差超阈值，§5.8）。

不通过原因码（4）：`MISSED_STEP`、`WRONG_STEP`、`OUT_OF_ORDER`、`DEADLINE_EXCEEDED`。

每个原因码在界面映射一句人类可读的排障提示。不引入第四态"降级判定"——VLM 不可用但流健康时直接不可判定 + `INFERENCE_BACKEND_UNREACHABLE`；第四态会让现场语义复杂化而收益有限。

**【已定】原因码集合会增长，新增为兼容变更，前端必须有未知码兜底。** 界面遇到不在自己映射表内的原因码时，显示原始码 + 一句通用提示（形如"该结论的原因码为 `XXX`，本界面版本尚无对应说明，请查阅版本说明或联系管理员"），并照常显示该结论的其余信息。**不得**因未知码而显示空白、显示"未知错误"、或把该条判定整条丢弃。

理由：`/api/v1` 不是版本轴（[ADR-0003](../adr/0003-api-v1-is-a-fixed-prefix.md)），没有 v2 供旧前端退守；而推理机与前端可能非同步升级，Q37（审计）与连接器相关变体都预示这个集合会增长。若前端把未知码当失败处理，一次原因码扩充就会让现场看到空白——而这条判定本身是有效的，操作员需要看到它。

因此契约兼容性检查中，**枚举加值不是破坏性变更**（前提是上述兜底成立），删值和改语义仍是。原因码的定义权在判定核心（`apps/edge-runtime/`）：它是原因码的产生者，OpenAPI 枚举与前端提示映射都由它派生，不各自维护一份。

**三值与违规锁存的关系**：违规一经确认即锁存，与所属实例的最终判定结果相互独立。实例可以是"不可判定"而同时挂有一条已锁存的违规——前者说"这一遍整体结论不可靠"，后者说"这个偏离是已被确认的事实"。两者都上报，界面分别呈现。

### 5.3 Excel → 模板版本 → 推理机的生成链路

```
平台 Excel（工位表 + 步骤表，保留原文件与导入记录）
  → 解析校验（步骤号连续、工位号存在、必填完整、编码规范）
  → 平台内模板草稿（可编辑的权威记录）
  → Studio 修改步骤、声明步骤顺序性、补运行参数默认值（空闲时限、超时时限、处置策略）
  → AI 生成提示词措辞 + 步骤对齐（调推理服务核对边界）[Q25 待确认]
  → 由持模板编辑权限的用户发布
  → 不可变模板版本（actions.json + vlm_prompts.txt + 运行参数默认值 + sha256）
  → 推理机按绑定拉取并落到本地，重启该后端进程生效
  → 推理机上报实际生效版本的 sha256（reported_version）
```

**`actions.json` 格式必须严配基座正则 `^\((\d+)\).+`**——`sop_step_checker` 用它提动作号，不匹配则 checker 静默失败。

**运行参数与顺序性声明是两类不同的东西，在 `template_version` 中是两个字段。** 运行参数是产品侧可调值（空闲时限、超时时限、处置策略），改它不改判定逻辑的形状；**顺序性声明是模板语义属性**——它决定步骤是否严格有序，从而决定跳号是否即判漏步（§5.1），改它改变判定逻辑本身。基座那三个阈值（0.6/0.3/0.8）在部署态不可配（§2.6），且只服务于被替换掉的边界启发式，二者都不属于，故不进模板版本。

**【已定】运行参数分两层：模板版本给默认值，工位持生效值，覆盖为整组替换。** 判定读的是工位上的生效值。

理由：这三项都是现场调参对象——空闲时限取决于该工位的节拍，超时时限取决于该工位的实际作业时长，处置策略取决于该工位有没有接执行器。若生效值只存在于不可变的模板版本里，调一次空闲时限就要发一个新版本并重新绑定，而模板语义（步骤、顺序性）根本没变；版本历史会被现场调参淹没，"回滚到某个历史版本"也就不再意味着回滚模板语义。

- `template_version.运行参数默认值` 不可变，是发布时冻结的建议值。
- `device_station` 要么没有覆盖组（整组取当前绑定版本的默认值），要么有一个**完整的**覆盖组（整组生效，模板默认值不参与）。**没有逐项合并**——只有两个状态，界面上就是"跟随模板"与"工位自定义"两档。
- 绑定新模板版本时，工位的覆盖状态不变：跟随模板的工位自动取新默认值；已自定义的工位继续用自己那组。
- 代价明确：**新版本调整了某项默认值时，已自定义的工位不会跟随。** 界面必须显示该工位处于自定义状态、其各项值、以及当前绑定版本的默认值供对照，使"该工位没跟上新默认值"一眼可见。逐项合并能免掉这个代价，但要在中心与界面各实现一套合并与来源标注规则，不值。
- 推理机拉取配置时拿到的是**已解析的生效值**一组，不是"默认值 + 覆盖组"两份。解析在中心完成，边缘不做选择。
- 覆盖是工位的配置变更，与其他配置变更同权限、同诊断日志事件，记录操作人与操作时刻。

这条使"模板声明默认值、现场调生效值"两件事同时成立且不矛盾，CONTEXT.md 的「空闲时限」定义已按此措辞。

**权威边界**：Excel 是可追溯的导入来源；解析成功后，平台内模板草稿是步骤与运行参数**默认值**的权威记录；运行参数**生效值**的权威是工位（见上）。重新上传 Excel 创建新的导入/草稿，不静默覆盖已有草稿。

**下发是拉取式**：推理机主动向中心拉取自己绑定的模板版本，中心不推。中心不可达时使用本地已缓存的最后一个已确认版本继续运行（§5.7）。

### 5.4 首切片控制面与 Web 基线

首切片只交付可操作控制面，不显示虚构运行状态：

- **模板**：导入规范化 SOP 文档、编辑模板草稿、发布、生成/校验/下载 `actions.json`、VLM prompt、运行参数默认值和 sha256。
- **工位运行参数**：每个工位可在"跟随模板"与"工位自定义"两档间切换；自定义时整组给值，界面同时显示当前绑定版本的默认值供对照（§5.3）。
- **身份与权限**：本地账户；系统管理员、设备管理员、模板编辑者、数据集管理者、运行操作员、复核人员。角色是权限集合，一个用户可拥有多个角色；LDAP/OIDC 后置。
- **工位与设备**：工位唯一编码/名称/标签；相机、推理后端和连接器支持持久化 CRUD、关联约束。允许离线保存；"测试连接"执行真实请求，结果为成功、失败或未验证，不返回模拟成功。
- **Web**：简体中文；桌面 Chrome/Edge 当前稳定版；1366×768～1920×1080；键盘可操作、表单有标签和明确错误、颜色不是唯一状态表达。导航为概览、工位与设备、SOP 模板、训练数据集、用户与权限；概览只显示真实配置摘要。
- **明确后置**：实时运行看板、违规处置、证据复核、大屏、统计报表、整改闭环、原生移动端和企业身份源。

#### 训练数据集与标注

**【已定】只允许上传视频，不接受压缩包，后端不解压。** 训练素材以单个视频文件逐个上传；视频是本产品拥有的客户资产，其归属、权限与生命周期归 `dataset`。随之作废的有：tar 成员安全校验、`dataset_member` 的"压缩包成员"语义、以及从已切片段反推 DDM 时间戳的转换器。

- **正式入口**：Web 与脚本调用同一应用 API 上传视频，不直接写数据库。上传经预签名 URL **直传 MinIO**，控制面只负责签发、登记与校验。预签名 URL 按 `dataset.dataset.import` 权限签发并限时。
- **`dataset` 拥有**：视频登记（来源、大小、sha256、时长、编码）、数据集分组、用途检查状态、派生制品引用。
- **动作时间段标注：原样复用基座 React 标注 UI。** 基座 `video-annotator-ms`（React + FastAPI）已实现时间轴标注、切片与训练触发。它经 Nginx 反代进统一入口，鉴权在网关补齐；**不用 Vue 重写**——那正是"两套东西"（§5.11、Q34）。标注是产生 DDM 所需 `{video_id: segments[]}` 的唯一途径。
- **一个必须知道的代价**：本地样本包内已有的切片（`server_fan/train/Install_N/`）与 VLM 微调数据（`augmented/`）不再能整体带入。片段本身是 mp4 可上传，但 DDM 需要"整段视频 + 各片段时间戳"，时间戳只能由标注产生。§2.7 的定序难题**移交给标注环节的人**。
- **VLM 用途检查**：校验 GQA/BCQ/MCQ/Golden GQA 结构、视频引用、动作编号及与当前基座提交的数据契约；通过后只标记"可用于后续微调"。首切片不启动 VLM 或 DDM 训练。

### 5.5 预览与录像（与 GPU 完全无关）

**海康 RTSP 地址格式**（已查证）：

```
主码流（判定用）: rtsp://<user>:<pass>@<ip>:554/Streaming/Channels/101
子码流（预览用）: rtsp://<user>:<pass>@<ip>:554/Streaming/Channels/102
```

注意事项（已查证）：密码含特殊字符必须 URL 编码（`@`→`%40`、`:`→`%3A`）；经 NVR 接入时通道号由 NVR 分配、不是 1；子码流特性是低码率、延迟稳定。

**⚠️ 拿到真机后必须首验**：子码流实际编码与 profile（`ffprobe` 看 `has_b_frames`）。WebRTC 不支持 B 帧（§2.11）——若子码流带 B 帧，先按具体型号能力尝试在摄像机 Web 配置中调整 codec/profile/GOP；**能关闭不兼容编码是零转码方案成立的前提**。配不掉才对该路启 x264enc 转码并在界面标注额外 CPU 开销。

```
相机主码流(101) ──→ 推理机推理服务容器（判定，GPU）
相机子码流(102) ──→ mediamtx（sourceOnDemand，零转码）
                     ├─→ WebRTC 单路详看（浏览器直连）
                     ├─→ WebRTC 多路大屏（浏览器直连，容量须实测）
                     └─→ 分段录像（保留 7 天）→ 违规时 ffmpeg -c copy 切片 → 上传中心 MinIO
```

排除推理服务 DeepStream RTSP 输出与 `ENCODE_VIDEO`（§2.9）。

**录像磁盘容量**：`子码流码率 × 路数 × 7 天 × 1.3`。余量系数覆盖 VBR 峰值、文件系统开销与"不跑满盘"三项，**算例必须含它**。按 1 Mbps 估算（1 Mbps × 604800 s = 75.6 GB 裸容量，×1.3 后）：

| 路数 | 裸容量 | 含余量（按此备料） |
|---|---|---|
| 1 | 75.6 GB | **~98 GB** |
| 8 | 605 GB | **~786 GB** |
| 50 | 3.78 TB | **~4.9 TB** |

这是推理机的硬件清单项，须在 C4 门禁前按实际码率复算。子码流实际码率高于 1 Mbps 时按比例放大。**压缩归档的节省不预先计入**（§5.19）：压缩比须在目标子码流上实测后才可写入本表，备料按不压缩计算。压缩带来的余量用于延长可配置的保留期，不用于缩减备料。

### 5.6 500 ms 的两个起点

同一个 500 ms 数字，按结果类型分两个起点——不拆则门禁由构造即失败（闭合类判定的"最后必要源帧"可能在空闲时限之前）：

| 结果类型 | 起点 | 涵盖 |
|---|---|---|
| **到达即判** | 形成该结果所需的最后一帧在相机侧产生 | 错步、反序、超时、顺序型模板的跳号漏步、实时状态更新 |
| **闭合类** | 闭合条件成立时刻 | 实例闭合时的集合比对结论 |

终点同为对应状态或判定完成 Web 渲染。闭合后只剩序列比对 + 写本地状态 + 上报 + 推送，500 ms 是合理预算。两类分别报告 p50/p95/p99、最大值与超预算计数。

判定在推理机本地完成，因此"判定产生"到"处置执行"不跨网络；跨网络的只有上报与 Web 推送。中心不可达时判定与处置照常，Web 看板停止更新——**看板延迟不属于实时防错的功能路径**。

### 5.7 推理机是自治判定单元

每台推理机上的组件：

```
推理服务容器（基座 + §5.11 一处改造；基座 checker 与处置按配置关闭）
  DeepStream 取流 → DDM 分段(Triton) → vLLM 分类 → SSE
  pipeline on_message → 合成健康事件 → _vlm_response_queue → SSE
本机 supervisor（自研，判定在此进程内）
  读本地配置，为每路已配置相机启动一个 /v1/chat/completions 请求并看护
  消费 SSE → 判定核心（序列比对+边界+有效性+三值）
  违规锁存 / 处置派发（含写输出点位）/ 证据切片指令 / 上报与对账
  chunk 静默计时器（空闲超时闭合 + 进程级失联兜底）
mediamtx（sourceOnDemand，零转码，7 天分段录像）
录像压缩归档任务（老化分段 H.264→H.265，NVDEC→NVENC，可限速可暂停，§5.19）
连接器运行时（轮询或推送输入点位 → 观测；执行输出点位写入）
本地状态存储（SQLite）
```

**为什么流租约不再需要**：租约（原 ADR-0001）存在的唯一理由是"中心多个进程可能重复订阅同一路相机，重复启 DeepStream pipeline 重复占显存"。判定回到推理机后，是本机 supervisor 驱动本机 pipeline，不存在跨机竞争者；"每路流有且仅有一个拥有者"退化为本机的进程管理问题。ADR-0001 因此作废。

**本地状态存储用 SQLite**：嵌入式、单机、标准库自带，不给推理机再加一个数据库服务。存本地配置缓存、已确认模板版本、在飞与已闭合实例、判定、已锁存违规、处置记录、待上报队列、待上传证据队列。

**中心不可达时的行为**：

| 能力 | 中心离线时 |
|---|---|
| 取流、感知、判定 | 继续（不受租约约束） |
| 违规锁存 | 继续 |
| 处置：记录 / 前端告警 / 声光 / 暂停工位判定 / 终止实例 | 继续 |
| 处置：**写输出点位** | 继续，直至物理执行租约过期（TTL 7 天，§5.17）；过期后降级并显式标注 |
| 证据切片 | 继续，本地缓冲排队 |
| 预览（浏览器直连 mediamtx） | 继续 |
| 模板版本变更、工位改绑 | 不生效，继续用最后已确认版本与归属 |
| 上报、看板、复核 | 暂停，恢复后对账 |

**对账语义**：上报按事件 id 幂等 upsert，至少一次；处置记录带幂等键，恢复后不重复执行已执行的动作；证据上传失败可重试且不删除本地唯一副本。版本握手在连接时进行，不兼容即显式告警并拒绝接入，不静默降级（[ADR-0003](../adr/0003-api-v1-is-a-fixed-prefix.md)）。

**健康状态的两种语义与写入者**（避免 `device` 与 `monitor` 各存一份"健康"）：

| 字段 | 语义 | 写入者 |
|---|---|---|
| `device_inference_host.健康状态` | 主机级**可达性**：中心能否连上这台机、上次上报距今多久 | 中心（心跳与上报时距，见下条自指问题） |
| `device_inference_backend.健康状态` | 进程级**可达性与就绪**：`/v1/live`、`/v1/ready` 的最近结果 | 中心（轮询基座健康端点） |
| `device_connector.健康状态` | 设备级**可达性**：最近一次测试连接或轮询结果 | 推理机上报（连接器运行时在本机，§5.8） |
| `monitor_stream_health` | **每路观测有效性**：连接/重连/断开时间线与时间锚定偏移，是三值判定的依据 | 推理机上报（源自 pipeline 回调，§2.4） |

前三者是"能不能连上"，`monitor_stream_health` 是"这段时间的观测能不能用于判定"。**判定只看后者**；前三者服务于运维界面与告警。看板把它们并排显示由适配层组合，不新建聚合模块。

**推理机自评健康的自指问题**：§5.2 不变量的四个条件全部由被评估的那台推理机自己产生。若该机部分子系统失能（磁盘满导致 `local_report_queue` 写入失败、supervisor 卡死）而判定输入链路表面正常，它仍会输出不通过，本机没有任何信号能发现这件事。故需要一个**不依赖被评估者自述的外部证人**：中心以"该机多久没上报"为独立判据，超阈值即在看板标注该机全部工位为可疑并告警。这不违反"中心不做二次探活"（ADR-0005）——中心判的不是流健康，是**这台机还活着吗**，二者信号来源与用途都不同。

### 5.8 连接器

连接器是**可配置集成点**，与相机、推理后端同级：能配、能不配、配了就用。判定逻辑不因某工位有无某类连接器而改变代码路径——这条是硬约束，代码中出现 `if 配了连接器: ... else: ...` 即为违反。

**连接器运行时在推理机内**，与判定同进程域：外部信号必须与动作编号并列进判定，物理联锁必须在本地闭环——停线信号绕中心跑一趟既加延迟又加故障点。配置由中心的 `device` 拥有并被推理机缓存；输出写入由本机 supervisor 的处置派发发起。

**时间对齐是配置了输入点位后的强制要求**：

| 观测来源 | 时间基准 |
|---|---|
| 动作编号（chunk） | `start_time`/`end_time` = 相对流开始的秒数 |
| 外部信号 | 主机时钟的绝对时刻 |

偏移不可用或漂移超阈值时，依赖外部信号的判定输出不可判定 + `IO_TIME_UNALIGNED`，**不得输出不通过**。这是 §5.2 安全不变量在这一维度的落地。

**【已定】连接器接口必须自带能力声明，判定按声明决策，不按适配器类型分支。**

不同适配器投递外部信号的方式差异会改变判定能力：海康 ISAPI 若只能轮询，轮询周期就是时间对齐误差下限，也就限制了该点位能承担哪类判定；板卡若能推送边沿，同一个点位才可能作为"到达即判"类结果的依据。这个差异不能藏在适配器实现里——藏起来就意味着换一个适配器会静默改变判定行为，而调用方以为它们可互换。

每个输入点位在配置阶段解析出一组能力声明，随点位一起进入模板绑定校验与判定：

| 声明 | 含义 | 判定如何使用 |
|---|---|---|
| 投递方式 | 推送 / 轮询 | 轮询时下条一并给出周期 |
| 最大投递延迟 | 从物理变化到观测可用的上界 | 与该点位承担的结果类型的 500 ms 预算相抵（§5.6） |
| 是否保序 | 同点位多次变化是否保证按序到达 | 不保序的点位不可作为顺序判断依据 |
| 是否可能丢边沿 | 两次采样之间的瞬时变化是否可能丢失 | 可能丢边沿的点位不可作为唯一的开始/结束信号 |
| 时间戳来源 | 设备时钟 / 主机接收时刻 | 决定时间锚定误差如何计入 `IO_TIME_UNALIGNED` 阈值 |

**校验发生在模板绑定时，不在判定运行时。** 模板把某点位声明为开始信号，而该点位的能力声明不足以承担该角色（例如可能丢边沿），绑定即失败并给出具体原因；不允许绑定成功后在现场以不可判定的形式表现出来。这样"判定逻辑不因有无连接器而改变代码路径"这条硬约束在下一层也成立：判定读的是能力声明这组数据，不是适配器的类型。

**能力声明是实测值，不是适配器作者的自我承诺。** 海康 ISAPI 的投递方式与延迟须由真设备测出后写入（本节待实测项即为此），板卡适配器到货后同样。声明与实测不符按缺陷处理。

**适配器与真硬件开发路径**（避开 mock）：海康摄像机自带告警输入/输出接线端子，ISAPI 暴露以下端点（已查证存在）：

```
/ISAPI/System/IO/inputs/<ID>          读写告警输入口配置
/ISAPI/System/IO/inputs/<ID>/status   读告警输入状态
/ISAPI/System/IO/outputs/<ID>/status  读告警输出状态
```

第一个适配器写海康 ISAPI，用真设备验证整条链路。板卡到货后只增加第二个适配器，上层不动。

**各适配器的能力边界要在设备管理页显式呈现**：摄像机告警口通常只有 1~2 路输入 + 1 路输出，继电器负载约 1A / DC30V。够接光电开关、按钮、指示灯，**不够做产线联锁**——停线联锁须用工控机板卡或经认证的 PLC/安全回路。

**待实测**：ISAPI 请求体格式；**是否有 `alertStream` 类推送端点**——这条决定该适配器的"投递方式"能力声明取推送还是轮询。若只能轮询，轮询周期即最大投递延迟的主要成分，并据此限制该点位可承担的结果类型（见上表）。

**默认取舍**：外部信号**不作为违规的唯一证据**——单点位信号无法区分"没按"与"线路故障"，与 §2.1 的教训同构。

### 5.9 基座契约测试与自有回归测试

改造后测试分两类，都在 `tests/contract/base/`，`git subtree pull` 后必跑：

**一、我们依赖但不改的基座行为**（前提失效检测）

| 断言 | 依据 | 测法 |
|---|---|---|
| 动作号提取正则 `^\((\d+)\).+` 未变 | §5.3 | 纯 CPU |
| chunk 必含 `chunk_idx`/`start_time`/`end_time`/`checker_result` | §2.10 | 结构断言 |
| `SopCheckerRequest` 构造签名未变 | 实测依赖（双跑对比用） | 纯 CPU 导入 + 签名检查 |
| `actions_can_be_skipped` 语义未变 | §5.1（首版不用该字段，但语义须钉住） | 纯 CPU |
| warmup 管线仍无条件加载 DDM（uniform 绕不开） | §2.7 | 静态检查 |
| DDM 标注字段（`start_timestamp`/`end_timestamp`/`description`）未变 | §2.7 | 静态检查 `ddm_dataset.py` |
| RTSP 初始错误重连配置仍存在 | §2.4 | 静态检查 `init-rtsp-reconnect-interval` |
| **`_vlm_response_queue` 的消费者集合未变** | §5.11（合成事件靠键区分，新消费者不检查该键即静默破损） | 静态检查该队列的全部 `get()` 调用点，出现未登记的消费者即失败 |

**二、我们自己实现或改造过的行为**（正确性 + 回归 + 补丁有效性）

| 断言 | 依据 | 测法 |
|---|---|---|
| **返工序列 `1,2,3,2,4,5` 判为通过，无 missing** | §2.2 | 纯 CPU，最高优先级回归 |
| 漏步在实例闭合时立即报出，不等下一遍 | §2.3 | 纯 CPU |
| 顺序型模板跳号即报漏步 | §5.1 | 纯 CPU |
| 流不健康期间的实例闭合为不可判定，不为不通过 | §5.2 | 纯 CPU，注入健康事件 |
| **我们的序列比对与基座在合规序列上结论一致** | §5.11 | 纯 CPU，双跑对比；**不含返工与漏步时机**——那正是我们故意不同之处 |
| 该处补丁能干净应用于当前 NVIDIA 提交 | §5.11 | 打补丁 + 导入 |
| 物理执行租约过期后不再写输出点位 | §5.17 | 纯 CPU，注入过期租约 |

**注意**："纯 CPU 可测"本身也是一条实测结论——基座那四个模块只依赖标准库。若 NVIDIA 给它们加了重依赖，"双跑对比"那条会先失败，这也是有效信号。判定核心只依赖标准库的规则（§5.11）保证我们这一侧持续可测。

**版本记录**：`docs/base/verified-commits.md` 记录每次 `subtree pull` 的提交号、契约测试结果、补丁是否需要调整、断言失败时的处置。

### 5.10 推理后端 = 一份模板配置的进程端点

**【已定】** `actions.json` 与 VLM prompt 是进程级配置（§2.10 实测），故**一个推理服务进程只承载一份 SOP 模板**。据此定义：

**推理后端是"承载一份模板配置的推理服务进程端点"，不是"一台推理机"。** 一台推理机可运行多个推理后端；共用同一后端的相机必须共用同一模板。

选这个模型而不是"一机一模板"，是因为它把后者当特例包住：若实测容不下多容器，退化为"一机一后端"只是部署约束，不改表结构；反之若先定"一机一模板"，将来要 per 工位模板就得改 `device` 与 `monitor` 两个模块的表。

三个连带后果：

- vLLM 是独立端点、可被多个后端共享；每个后端的额外常驻开销是一份 DDM Triton 实例 + CUDA context。**该开销须在 G2 门禁单列**，它是容量公式的一项。
- G4 门禁改测「N 后端 × 1 路」，原「1 后端 × N 路」测不到真实部署形态。
- `device_camera` → `device_inference_backend` 外键语义随之明确：指向进程端点，不指向物理机。物理机由 `device_inference_host` 表达。

### 5.11 基座姿态：一处改造 + 一处自己实现 + 两处配置关闭

**语言前提**：NVIDIA 仓库的代码是**本系统的躯干**。文档中不称"上游"，只在描述 `git subtree pull` 的版本来源时称 **NVIDIA 仓库**。

**【已定】分类清单**（[ADR-0007](../adr/0007-base-is-the-trunk-not-a-dependency.md)）：

| 姿态 | 范围 |
|---|---|
| **原样复用** | DeepStream 取流、DDM 分段、vLLM 分类、`/v1/*` 接口、文件 API、Prometheus 指标；训练侧 5 个微服务；**React 标注 UI 连界面一起复用** |
| **就地改造（一处，纯加输出）** | pipeline `on_message`：把 source error / 正在重连 / 重连成功 / 最后一帧时刻 / 时间轴归零作为**合成健康事件**送进 `_vlm_response_queue` |
| **自己实现（一处）** | 序列比对 + 声明式边界 + 有效性门 + 三值判定，全部在 `apps/edge-runtime/`，`vendor/` 不留补丁 |
| **配置关闭（不打补丁）** | 基座 checker（`DISABLE_SOP_CHECKER=true`）；基座处置（`ENABLE_ALERT_SOUND`/`ENABLE_MESSAGING` 保持默认 false） |
| **全新建设** | 账户权限、工位/相机/连接器配置、Excel→模板版本发布、聚合看板、违规复核、证据生命周期、上报与对账 |

该处就地改造是在回调点**追加调用**，不进入他人控制流，故"最小 hook 在 `vendor/`、逻辑在 `apps/edge-runtime/`"这一分工成立；不动 DeepStream/Triton/vLLM 的计算路径，NVIDIA 的性能改进与 CUDA 版本适配仍可 `subtree pull` 进来。

**判定核心在 supervisor 进程内，不在基座进程内**（[ADR-0005](../adr/0005-judgment-runs-inside-the-inference-host.md)）。supervisor 消费本机 `/v1/chat/completions` 的 SSE 流，在自己的进程里调用判定核心。基座 checker 用既有环境变量关掉，不打补丁替换其调用点——那是控制流替换，且承载它的循环是 `_vlm_response_queue.get(block=True)`（`ds_sop_process.py:715`，无超时），空闲超时在其中永远触发不了。

**为什么处置不再是一处改造**：基座处置在 `post_dispatch_process`（`ds_sop_process.py:800-813`），是独立线程，对每个 chunk **无条件**触发 `alert_sound` 与 `messaging_chunk`，无违规过滤、不持有判定结果。"扩成 5 种动作"要先教它认判定结果，而 supervisor 天生持有判定结果，且 §5.7 本来就把处置派发指派给 supervisor。两处响应面均由环境变量开关且默认关闭。

**为什么序列比对不打补丁**：`missing_number_detector.py` 中周期边界判断（`:147-188`）与序列比对、状态更新交织在 `process_number` 同一方法内——`_complete_cycle()` 调用、`seen_in_cycle` 清空、`result` 填充全在其中。换边界来源必须整体覆写该方法，"最小 hook"对它做不到。该文件全文 539 行、只依赖标准库，自己实现比在他人状态机内解冲突更省。

这不违反"不许两套实现"：我们的判定路径上只有一套（我们的），基座那份被 `DISABLE_SOP_CHECKER` 关闭后连线程都不启动（`:652`），`inference_last_queue`（`:592-598`）直接返回 `_vlm_response_queue`。

**合成健康事件的注入点（已实测）**：三个候选 sink 只有一个可用。`_boundary_queue` 不可用——`clip_post_process:923` 按 `(frame_id, pts, score)` 位置解包；`_chunk_queue` 不可用——`vlm_inference_request_process:1014` 要求数值 `start_time`/`end_time` 并会对它跑一次 VLM 推理；`_vlm_response_queue` 可用——`:1136` 对 `response_future` 有默认值与判空。故补丁是**两个触点、两个文件、均为纯追加**：`ds_sop_process.py:556` 传入 sink，`ds_3d_action_pipeline.py:779` 的 `on_message` 追加一次 hook 调用。合成事件用显式键（如 `stream_health`）标记，不用哨兵数值。沿用基座自己的做法——它在流结束时就造过 `chunk_idx=-1` 的合成 chunk（`:719-730`）。

**该通道只在进程与 pipeline 存活时能投递。** 它带序送出可恢复状态（source error、正在重连、重连成功、时间轴归零），"该事件发生在 chunk N 与 N+1 之间"可直接用于闭合时的有效性判断。进程死亡送不出任何东西，那种情形由 supervisor 的 **chunk 静默计时器**兜底——后者也是空闲超时（§5.1）所需的同一个计时器。

**代码放置**：`apps/edge-runtime/` 是独立 Python 包；`vendor/` 内只留一处最小 hook。我们的代码因此可被 lint、type check 与单元测试覆盖。补丁维护为可重放 diff，`subtree pull` 后由契约测试验证仍可干净应用。

**依赖约束（硬规则）**：判定核心只依赖 Python 标准库。判定核心运行在 supervisor 进程里，故这条不再由运行环境强制，而是为**可测试性与可移植性**保留：成本近零，且保证判定核心可纯 CPU 测试——基座那四个模块本来就是这样。`vendor/` 内那处 hook 仍受运行环境强制，因为它确实跑在 DeepStream 容器内（§2.10）。

被否决的姿态：**全量只读**会让中心再建一套判定与探活，而第一手信号在推理机（§2.4），必然产生两套不一致的实现；**全量吸收**低估 GPU 基础设施维护成本，等于自建一个 DeepStream 维护团队；**判定进基座进程**见 [ADR-0005](../adr/0005-judgment-runs-inside-the-inference-host.md) 的三条实测理由。

### 5.12 凭据在推理机本地

**凭据在推理机本地录入、本地加密存储、本地校验。** 中心只存"该相机凭据已配置/未配置"的状态标志，不存密文也不代管（[ADR-0008](../adr/0008-credentials-stay-on-the-inference-host.md)）。

理由：中心一旦代管凭据，推理机重启就依赖中心可达——与 §一 的自治约束直接冲突。中心也就自然满足"不提供明文读取接口"，因为它从来没有过密文。

- 中心生成的推理机配置骨架**不含秘密**，只有占位符与说明。
- 推理机本地密钥由部署时挂载的只读 secret 文件提供，不进入 Git、日志、API 响应或导出。
- 凭据写入后只能覆盖或用于本机真实连接测试，不提供明文读取接口。
- 诊断日志只记录操作人、目标、时间和结果，不记录秘密值。

### 5.13 模型权威在推理机

**推理机部署 vLLM，型号可自定义，推理机是模型的权威。** 中心通过基座已有的 `/v1/metadata` 与 `/v1/models` 读取该机实际加载的模型标识，写入判定溯源。不建中心模型注册表模块，不做中心分发。

代价须说明：判定溯源里的模型版本从"中心保证的绑定"降级为"推理机自报的事实"。对内网单工厂交付够用；若将来要求"某工位必须用某个 checkpoint 才能判合规"，再加一致性校验。DDM checkpoint、VLM 权重的许可闭包与离线可分发性仍是 L1/L2 门禁项，由部署流程而非中心模块承担。

### 5.14 异步任务用 ARQ，不立独立 Worker 进程

piko 立独立 Worker 进程的三条前提在本项目**全部不成立**：后台本身就是 Python/FastAPI（无语言边界）；GPU 被推理机独占，中心不做 GPU 工作；中心读不到推理机显存压力，做不到限速/暂停。

但**异步长任务需求是真的**：步骤对齐要调推理跑多遍模板视频，十分钟级；训练编排更长。Excel 解析反而是亚秒级，不需异步。

→ 用 **ARQ 任务队列**（Redis 已在栈内，原生 async、依赖轻）。

**【已定】投递语义：PostgreSQL 为权威，Redis 只作传输**（[ADR-0004](../adr/0004-postgres-is-the-job-authority.md)）。`job` 模块拥有 `job_application_job` 表；入队走事务性 outbox（与业务变更同事务写 job 行，提交后投递 Redis），worker 按 job id 幂等执行，语义为至少一次。不得直接调用 `arq.enqueue` 绕过 outbox。**补投由 ARQ cron 任务扫表完成，不新增进程。**

ARQ 只处理导入校验、制品生成等后台任务，不进入实时判定链路——实时链路整体在推理机内。

### 5.15 控制面契约基线

以下为前后端共同起步所需的最小契约，是**跨模块统一纪律**，不是逐资源的实现细节。

**资源身份**：对外身份是服务端生成的 UUIDv7。自然键（工位编码、登录名）保留唯一约束，作为查询过滤与导入匹配键，**不作为 URL 身份**。

**错误契约**：RFC 9457 `application/problem+json`，扩展成员 `error_code`（稳定 SCREAMING_SNAKE 枚举）与 `field_errors[]`。**`error_code` 与 `reason_code` 是两个不同类型**：前者是 API 调用失败的原因，后者是判定结果的领域理由（§5.2）。二者在 OpenAPI 中是两个枚举，禁止共用字段名——混用会让"不可判定"被前端当成 HTTP 失败处理。两者都要求前端有未知值兜底：`reason_code` 按 §5.2 显示原始码 + 通用提示，`error_code` 按 HTTP 状态与 `title` 降级显示。

**授权**：权限决策归 `auth`。**`authorize` 调用点在各模块的 `usecases/` 层**（用例边界），不在 HTTP 路由——ARQ worker、上报入口与 smoke 脚本调用同一批用例，只在路由强制等于让权限依赖调用方自觉。路由上的权限声明是元数据（供 OpenAPI 展示），由机械检查保证「每个写路由都声明了权限且与用例实际检查一致」。权限命名固定三段 `module.resource.action`，在代码中是枚举而非裸字符串；粒度为 `view` / `edit` / `delete`。

**不做审计事件**：不建独立审计表。变更归属由各实体的 `created_by` / `updated_by` / 发布者列承担；操作痕迹由诊断日志承担。这是明确取舍：放弃"业务事实与业务变更同事务、不可丢、append-only"的强度，换取模块与运维面更简单。若商业交付要求合规审计，需重新引入并承担相应改造。

**诊断日志**：structlog JSON 输出，五个必填字段 `event` / `module` / `correlation_id` / `level` / `ts`。**`event` 是稳定 snake_case 标识符**（如 `template.version.publish.rejected`），检索靠它而非文案。禁止裸 `print` 与 `logging`，由 ruff 规则保证。不建事件名注册表。日志配置与 correlation id 传播属共享基础设施（`factory_sop/observability/`），被所有模块 import，自己不 import 任何领域模块，由 `import-linter` 保证；**它不是一个领域模块**——不拥有表，也不拥有领域行为。

**并发写**：模板草稿等可被多人编辑的记录带整型 `revision`，写请求携带 `If-Match`，不匹配返回 409 + `error_code=STALE_REVISION`。理由：静默丢一条步骤的后果是"已发布模板少一步"，而漏步正是本系统要检出的违规类型。

**停用与删除**：可变配置对象（工位、相机、推理后端、连接器、用户）用**停用**表达下线，可逆，历史记录与关联关系完整保留；**删除**是另一项按权限开放的操作。模板版本不适用停用——它是不可变记录，回滚即重新绑定历史版本。停用用户必须同时吊销其全部会话。

**长任务进度**：REST 轮询 `GET /api/v1/jobs/{id}`。看板 SSE 在运行切片随上报入口一起落地。

**推迟的契约（显式清单，非沉默省略）**：推理机→中心的上报契约（`ReportedDecision`、`ReportedViolation`、`ReportedHealth`）与中心→推理机的拉取契约（`TemplateBundle`、`DeviceConfigBundle`）留到运行切片，用真实录制 chunk 与真机行为确定。seam 的名称、拥有模块与落表位置已在 §六/§七 固定。`packages/contracts/` 首切片只放导出的 `openapi.json`。理由：ADR-0003 的兼容性门禁为保护真实调用方而设——把未见过真实数据的猜测放进去只会锁死自己。

**但拉取契约的作用域现在就定：按请求方主机身份裁剪。** `DeviceConfigBundle` 只含该推理机自己绑定的工位、相机、推理后端、连接器与点位，以及这些工位的运行参数生效值（§5.3 已解析，不是默认值加覆盖组两份）与物理执行权状态；不含其他推理机的任何拓扑。`TemplateBundle` 同理，只含该机绑定的模板版本。

理由有两条，任一都足够：ISP——推理机不需要知道全厂拓扑，多给的字段都是它必须忽略而又可能误用的输入；安全——[ADR-0008](../adr/0008-credentials-stay-on-the-inference-host.md) 刚把凭据下沉到本机以缩小秘密的暴露面，若配置束回头把全系统部署结构发给每台机，一台被拿下的推理机就能读出全厂工位与设备清单。裁剪须可测：契约测试断言"以主机 A 的身份拉取，响应中不出现任何仅属于主机 B 的标识"。

**通用约定**：时间戳全部 UTC RFC3339（`Z`），Web 按 Asia/Shanghai 渲染；枚举在线格式为稳定 snake_case 字符串，不用数字；列表统一信封 `{items, page, page_size, total}`。

**验收场景**：以场景表（ID + 前置 + 动作 + 可观察结果）形式随各实施切片给出，`tests/system/` 用同一 ID 命名测试，使"哪条验收未实现"可 grep。不引入 Gherkin 层。

### 5.16 模块形状与切片顺序

**【已定】`api.py` 是跨模块契约，不是本模块的对外入口全集。** 每模块有一个用例层（`usecases/`），授权在此强制；`api.py` 只暴露**其他模块真正调用的那个子集**。例如 `device` 的约 40 个配置用例中，只有 `resolve_station_topology`、`report_health` 这类会被 `template`/`monitor` 调用的进 `api.py`，其余停在用例层。

理由：`api.py` 的存在理由是限制跨模块耦合面。把只有 HTTP 一个调用者的用例也塞进去，会让"跨模块契约"与"对外 API 全集"混成一个文件，`import-linter` 也就无法再告诉我们哪些行为真的被别人依赖。反过来，若让配置类模块的 CRUD 一律穿过 `api.py`，得到的是 HTTP handler → `api.py` → repository → 表，中间那层不贡献任何东西。

**【已定】首切片纵向推进，两个例外先落地。** `auth`（所有切片的授权都依赖它）与基座契约测试（纯 CPU 无依赖）先做；之后按 `device` → `template` → `dataset` 逐个端到端（表 + 迁移 + 用例 + API + 前端页）完成再下一个。

理由：横向分层会让第一个可演示的东西出现在整个首切片末尾，而多处待实测项（海康子码流 `has_b_frames`、ISAPI 有无推送端点、标注定序）只有走到那一层才会暴露。代价是迁移历史更碎，但单一线性历史 + 文件名前缀（§六）使这代价可承受。

**【已定】判定核心在首切片实现，作为 `apps/edge-runtime/` 的纯函数模块。** 输入是自己拥有的 Python 类型，无 HTTP 入口、无表、无生产者。

理由：§5.2 的安全不变量与 §5.1 的边界逻辑是整个产品最高风险的部分，而它们完全不需要 GPU 或真实数据就能测（§2.1、§2.2 已实测）。推到运行切片等于让最危险的逻辑与最难调试的环境（GPU + 真实流 + 时序）同时首次出现。§2.2 的返工用例就是首切片第一个要通过的测试。**代价：首切片存在一个不可从 Web 到达的模块**，这要在切片验收里写明，不得被当成"未完成"。

**【已定】前端按导航项划分五个模块**（概览、工位与设备、SOP 模板、训练数据集、用户与权限），不按后端模块划分。**概览的跨模块聚合由 HTTP 适配层组合**：各模块用例层各出一个 `summary()`，适配层在同一请求级 UoW 内调用并组装响应。

理由：新建 `overview` 模块会拥有一份不属于自己的数据视图，是典型的表形状模块；让某个现有模块跨界读表直接违反所有权规则；前端并发调四个接口则把"什么构成概览"的业务定义泄漏到浏览器。适配层做纯组合（不含判断逻辑）本就是它的职责，且 ADR-0002 已让它持有请求级 UoW。

**标注 UI 是例外**：基座 React 标注 UI 原样复用，经 Nginx 反代，不纳入 Vue 五模块划分。

### 5.17 工位改绑与物理执行权

判定回到推理机后，"每路流有且仅有一个拥有者"退化为本机进程管理问题（§5.7）。但**"哪台推理机有权驱动这个工位的执行器"不因此消失**——它是跨机问题，且后果比重复占显存严重得多。

失效场景：工位 A 原绑推理机 1，凭据、模板、连接器点位全在机 1 本地生效。运维在中心把工位 A 改绑到机 2，但机 1 未被物理下线（网络故障或运维遗漏）。因"中心不推、推理机拉"（§5.3），中心无法通知机 1 交权；机 1 靠本地缓存继续判定与处置，两台自治单元同时对同一停线联锁点位发指令。

**【已定】握手为主、租约为辅，归 `execution` 模块拥有。** 该模块强制的不变量是：**任一工位在任一时刻最多一台推理机持有未过期的物理执行权。**

- **改绑握手**：中心侧改绑操作**默认阻塞**，直到旧推理机确认已停止该工位的判定与处置并释放点位。旧机在下次拉取配置时看到"该工位已转出"，停止该路 pipeline、释放连接器点位、上报交权确认；中心收到确认后才允许新机接管。
- **强制改绑**：旧机永久失联（硬件损毁、报废）时提供强制通道，**需二人确认**（操作者 + 另一名持 `execution` 强制改绑权限的用户，该权限默认授予设备管理员角色），显式警示"若旧机仍在运行将造成重复物理写入"，并产生诊断日志事件记录两名确认人。这是全系统唯一要求双人的操作——因为它是唯一可能同时驱动两套物理执行器的操作。
- **物理执行租约（兜底）**：写输出点位需持未过期租约，租约在每次成功拉取配置时续期，**TTL 为 7 天**。过期后该机降级为"只判定、只上报、不写点位"，界面显式标注该工位物理防错已失效。

**为什么 TTL 取 7 天而不是 24 小时**：TTL 短会拿例行风险换失误风险。中心周五晚故障、周一才修是例行情况，24h TTL 会让所有推理机在周末无人值守时段静默失去物理防错能力；而"改绑却不下线"是操作失误，已由握手与双人确认拦住。租约只负责"旧机永久消失且未走强制通道"这一残余情形，7 天足够运维发现。

**判定与上报不受租约约束**，可无限期靠本地缓存继续——只读行为不会造成物理冲突，而这正是自治的价值所在（§一）。

### 5.18 判定核心的接口形状

**【已定】判定核心是纯状态转移函数：`(状态, 事件) → (新状态, 判定输出, 下次唤醒时刻)`。核心内不读钟、不查状态、不做 I/O。** 时间只作为事件上的数据进入；状态由 supervisor 持有并持久化，每次调用传入、拿回新值。

这条决定的是首切片最高风险模块的可测性，故写实到接口形状而非只写原则。

**为什么核心不能自己读钟**：§5.1 的空闲超时与 §5.2 的超时违规都需要时间，但一旦核心内出现"现在几点"，它的行为就依赖调用时刻，返工用例（§2.2）这类回归测试要靠 mock 钟或 sleep 才能写——而这正是我们把判定从基座进程里搬出来所要避免的那种不可测。核心内允许对入参里的时间戳做算术（那是数据），不允许获取当前时刻。

**事件种类**（supervisor 归一化后送入，核心不知道它们来自 SSE、连接器还是计时器）：

| 事件 | 携带 | 核心用它做什么 |
|---|---|---|
| 观测 | 动作编号或外部信号语义标签、源时间、主机单调锚 | 开实例、序列比对、顺序型跳号即报、超时违规 |
| 有效性事实变更 | 流健康事件或推理可达性变化，及其在事件序中的位置 | 累积进实例的有效性记录，闭合时供有效性门查 |
| 计时器到点 | 触发时刻的单调值，以及 supervisor 当时查得的进程存活与流健康 | 空闲超时闭合、超时违规 |
| 运行中断 | 原因（配置切换 / 进程重启） | 在飞实例结案为不可判定 + `RUN_INTERRUPTED` |

模板（含步骤、顺序性声明、已解析的运行参数生效值）随状态一起传入，是数据不是配置读取。

**判定输出**含：三值结果与原因码、所属实例、是否闭合该实例、需锁存的违规、需要的证据切片区间。**核心只描述该做什么，不执行任何一件。** 锁存、处置派发、证据切片、上报、写 SQLite 全在 supervisor（§5.7）。

**下次唤醒时刻由核心声明，计时器由 supervisor 持有。** 核心根据当前状态与运行参数算出"下一个需要被唤醒的单调时刻"（空闲超时或最近的步骤时限，取先到者）并返回；supervisor 按它设一个计时器，到点时查明当时的进程存活与流健康，作为"计时器到点"事件送回。

这样安排的理由：**违规判定权完整留在核心**——`DEADLINE_EXCEEDED` 与其他三类违规同源，若让 supervisor 自己判超时，违规判定就分裂在两处，而它们共享同一份实例状态。核心又确实不能唤醒自己，所以只声明、不持有。§5.7 里"空闲超时闭合 + 进程级失联兜底"因此是同一个计时器的两种到点情形，区别只在 supervisor 到点时查得的事实不同，核心侧无分支。

**这条不改变已定的其他事项**：判定仍在 supervisor 进程内（ADR-0005），核心仍只依赖标准库（§5.11），空闲时限生效值仍属工位（§5.3）——只是它以数据形式进入核心，核心不去读配置。§5.6 的"闭合条件成立时刻"因此在代码里有了确定对应物：计时器到点事件所携带的那个单调值，或使闭合条件成立的那条观测的单调锚。

**对测试的影响**：§5.9 每一条自有回归断言都退化为"构造状态 → 送若干事件 → 断言输出"，无钟、无夹具进程、无 sleep。§2.2 的返工用例是纯序列输入；"流不健康期间闭合为不可判定"是送一个有效性事实变更再送一个计时器到点。

### 5.19 数据保留与压缩

**【已定】保留策略可配置，按判定类别分类，由 `retention` 模块拥有策略、由数据拥有者执行删除。**

#### 总则：时长一律是配置，代码内不得出现字面量

**本节及全文出现的每一个时长数字都是默认值，不是常量。** 判定核心、supervisor、回收任务、mediamtx 配置生成中都不得出现硬编码的保留时长——包括看似"显然"的 7 天、24 小时、90 天。它们全部经配置解析后作为数据传入（判定核心尤其如此：它不读配置，见 §5.18）。

由机械检查保证，不靠评审：**回收与切片相关代码路径禁止时间字面量**（ruff 自定义规则或等价检查，与 §5.15 禁止裸 `print`/`logging` 同一手法）。默认值集中在一处配置定义里，改默认值是改那一处，不是搜索替换。

首版默认值一览（**全部可改**，均为按工程惯例取的默认值）：

| 项 | 默认 | 归属 |
|---|---|---|
| 录像滚动窗口 | 7 天 | 推理机 |
| 录像压缩起始年龄（= 原始素材窗口） | 24 小时 | 推理机 |
| 证据保留：通过类 | 90 天 | 全局默认，工位可覆盖 |
| 证据保留：不通过类 | 永久 | 同上 |
| 证据保留：不可判定类 | 90 天 | 同上 |
| 观测/判定明细保留 | 90 天后聚合归档 | 全局默认 |
| 记录压缩起始年龄 | 7 天 | 全局默认 |
| 违规与处置记录 | 永久 | 全局默认 |
| 推理机本地已上报数据 | **14 天** | 全局默认 |
| 片段窗口前/后余量 | 5 s / 5 s | 全局默认，工位可覆盖（§5.20）|

"永久"是一个显式可选值，不是"没设置"。界面必须能区分二者。

**推理机本地 14 天的取值理由**：本地状态是排障依据与断网缓冲，不是数据仓库——权威副本在中心（明细 90 天后聚合归档）。14 天覆盖"周五坏、周一修"那一类中心离线（与 §5.17 物理执行租约取 7 天同一类现实），并留出一倍余量供运维发现积压；再长则边缘盘变成第二个数据仓库，而 Q36 已定推理机 SQLite 不入备份，留更久也不增加可靠性。**上报队列与证据队列不受此期限约束**——它们是唯一副本，只在成功上报并确认后才删（§5.7）。

**上限与下限须有校验**：设成 0 或负数、设成超过录像滚动窗口的证据再切片期望、把明细保留期设得短于压缩起始年龄，都是配置错误，保存时即拒绝并说明原因，而不是运行时表现为怪异行为。

#### 五类数据面各自的保留语义

| 数据面 | 位置 | 保留由谁执行 | 可配置项 |
|---|---|---|---|
| 连续分段录像 | 推理机 mediamtx | mediamtx 保留策略 + 边缘运行时的压缩归档任务 | 滚动窗口时长（默认 7 天）、是否压缩归档、压缩起始年龄（默认 24 小时）|
| 证据（片段与关键帧） | 中心 MinIO | `evidence` | 按判定类别分别设时长；可设为永久 |
| 观测与判定记录 | 中心 PG（hypertable）| `monitor` | 明细保留时长、压缩起始年龄、聚合归档年龄 |
| 违规与处置记录 | 中心 PG | `alert` | 保留时长；默认永久 |
| 推理机本地状态 | 推理机 SQLite | 边缘运行时 | 已上报数据的保留时长 |

**滚动录像不参与分类。** 录制发生在判定之前，做不到"只录某一类"。分类作用于**从滚动录像提升为长期证据**那一步（§5.5 的 `ffmpeg -c copy` 切片），即：哪些判定类别触发提升、提升后留多久。滚动窗口本身对所有路统一。

#### 判定类别是三类，不是 OK/NG 两类

保留类别与 §5.2 的三值判定同构：**通过 / 不通过 / 不可判定**。

不可判定必须是独立一类而不能并入任何一边：它是本系统区别于基座的核心能力（§2.1），也是排障价值最高的一类——`不可判定 + STREAM_LOST` 时运维要靠画面判断是网络抖动还是相机故障。把它并入"通过"会丢掉排障依据，并入"不通过"会污染合规统计。

**挂有已锁存违规的实例按"不通过"类保留，无论其实例判定结果为何。** §5.2 已定违规锁存与实例判定相互独立，实例可以是不可判定而同时挂一条已锁存违规；违规是已确认的事实，其证据必须留。

**通过类默认也保留（可配置为不留）。** 合规率需要分母：只留不通过会让"合规率"无法计算，只能得到违规绝对数。选择不留通过类证据的部署要在界面上明确这一后果。

**不可判定类的证据是断点之前的片段。** 断流后没有画面可切，`STREAM_LOST` 的证据就是"录像在该时刻终止"这一事实本身加上终止前的片段。

#### 压缩：录像重编码归档，证据不动，记录用 Timescale 原生压缩

**压缩的对象是录像文件，不是实时检测路径。** 二者的约束完全不同：实时路径受 500 ms 预算与 §2.8 的转码瓶颈约束（8 路软编吃 ~10 核，50 路不可行），归档压缩不在任何延迟预算内、可延迟到低谷时段、可用硬件编解码通路。

| 对象 | 压缩 | 做法与理由 |
|---|---|---|
| **连续分段录像** | **做** | 全系统最大的存储项（§5.5：50 路 7 天约 4.9 TB）。已老化出"原始素材窗口"的分段重编码后原位替换。**编码格式受再切片回看约束，见下** |
| 证据片段与关键帧 | **默认不做** | 完整性优先，见下 |
| 观测/判定/上报记录 | 做 | TimescaleDB 原生 hypertable 压缩按年龄自动生效，JSON 文本压缩比 5~10×，`monitor` 本就规划 Timescale（§六），不引入新组件 |
| 推理机 SQLite | 不做，只裁剪 | 嵌入式库压缩收益小、复杂度高，直接删更干净 |

**压缩是让保留期可延长的前提。** 这两个需求是一件事：7 天窗口在 50 路下已占 4.9 TB，要延到 30 天就是 21 TB，不压缩则保留期无法配置得更长。压缩省下的容量直接换成可配置的保留时长。

**录像压缩的五条约束**：

1. **延迟于原始素材窗口。** 分段必须先度过一个"原始素材窗口"（默认 24 小时）才可被压缩。该窗口内的证据切片与再切片（§5.20）从**未重编码的原始分段**取，`-c copy` 无损；压缩必须落在其后。
2. **编码格式必须保持浏览器可直接回看。** 归档压缩后的分段仍是再切片的素材来源（§5.20），而再切片产物要在复核界面里播放。**默认用 H.264 提高 CRF 而非换 H.265**：H.265 在 Chrome/Edge 的支持依硬件与平台而异，不可靠；若归档为 H.265，再切片就得再转一次码才能回看，等于三代编码。节省少一些，换取"再切片产物 `-c copy` 即可回看"这条性质。想用 H.265 的部署必须接受再切片时的额外转码，并在界面标注该片段已多代编码。
3. **不得与判定争资源。** 走 NVDEC→NVENC 全硬件通路时，编解码用的是独立于 CUDA 核的硬件单元，与推理争用主要在显存带宽与显存容量上，而非算力。但**这一点尚未在目标 GPU 上实测**，故它是门禁项而非已知事实（见下）。任务必须可限速、可暂停、可中止，且判定侧出现积压时自动让路。
4. **原位替换须先验证后删除。** 重编码产物校验通过（时长一致、可解码、帧数在容差内）才删原分段；失败则保留原文件并告警。中途断电不得同时丢掉两份。
5. **【已定】压缩画质损失可接受，故 CRF 由实测扫出而非保守取值。** 压缩比与耗时是实测值：子码流本来就是低码率，提高 CRF 的实际节省须在真机上按目标子码流实测。既然画质损失已被接受，阶段 1 的实测**扫一组 CRF 找到复核可用性开始劣化的那一档，取其前一档为默认值**，而不是测单一保守设置——保守取值会白白丢掉可换成保留期的容量。

   **唯一的下限是功能性的，不是画质指标**：压缩后的分段仍须支撑人工复核判断与再切片（§5.20）。压到看不出发生了什么，再切片这个功能就失去意义，`不可判定 + STREAM_LOST` 也无法用画面区分网络抖动与相机故障（§5.19 分类保留的用途之一）。故验收标准是"复核人能据此下结论"，由人判断，不由 PSNR/SSIM 之类客观指标代替。

   §5.5 的备料表在实测前按**不压缩**计算，压缩节省不预先计入；实测后按 CRF 定档复算，节省用于延长可配置保留期。

**为什么证据默认不重编码**：证据是支撑违规判定的凭据，Q4 保留了 sha256 完整性校验，重编码会改变文件从而使原始摘要失效——"已被确认的事实"的载体被系统自己改写过，在合规追溯上是自伤。且证据切自子码流且 `-c copy`（§5.5），单体本来就小。若某部署的证据总量确实成为瓶颈（长期保留下会累积），可作为显式配置开启，但必须同时记录原始与重编码后两个摘要并在记录上标注"已重编码"，不能静默替换。这条留到有真实容量压力时再定，现在不做。

#### 变更保留策略是破坏性操作

- **缩短保留期只对将来生效**，不立即批量删除历史数据；实际删除由后台任务按新策略逐步执行，可观察进度。
- **提交前必须显示影响**：将删除多少对象、占多少空间、最早与最晚时间范围。不显示影响的确认按钮等于让人盲签。
- **不得删除仍被引用的数据**：仍关联开放违规、在审复核或未结案 SOP 实例的证据一律跳过并计数上报，不因保留期到期而删。这是 `retention` 模块强制的跨模块不变量，也是它独立成模块的理由——该约束横跨 `evidence` 与 `alert`，散在各模块就无人能强制。
- 策略变更产生诊断日志事件，记录操作人、变更前后值与影响估算。
- **与备份的关系必须显式**（关联 Q36）：备份保留期长于数据保留期时，删除只是"在线不可见"而非真正删除。二者的关系要在界面上写明，不能让运维以为数据已彻底删除。

#### 策略的粒度

**全局默认 + 工位覆盖，覆盖为整组替换**，与运行参数同一模式（§5.3）：工位要么整组跟随全局默认，要么整组自定义。理由相同——逐项合并要在中心与界面各写一套合并与来源标注规则，收益不足；而"这个工位的保留策略是什么"必须一眼可答。

录像滚动窗口是**例外，按推理机配置**：它受该机磁盘容量约束（§5.5 容量公式），而磁盘是主机属性，不是工位属性。

#### 磁盘水位是安全问题，不只是运维问题

§5.7 已识别推理机自评健康的自指问题，并明确举例"磁盘满导致 `local_report_queue` 写入失败而判定输入链路表面正常"。保留策略是这个风险的第一道防线，但不是全部：

- **上报队列与证据队列永不因空间回收而删除**——它们是唯一副本（§5.7 已定）。
- 磁盘水位越过阈值时该路进**不可判定**并显式告警，不静默写失败。这是 §5.2 安全不变量在存储维度的落地：写不下证据就等于观测能力不完整。
- 水位阈值与当前用量在设备管理页可见，与录像容量公式（§5.5）并排显示。

### 5.20 证据片段窗口与再切片

**【已定】片段窗口是可配置的前后余量，锚点由判定核心声明。再切片可用，但受滚动录像窗口限制。**

#### 窗口的形状：锚点 + 前后余量

片段不是"从某时刻起 N 秒"，而是**围绕一个语义锚点向前后各留一段余量**。余量是配置项（默认见 §5.19 总则表），全局默认可被工位覆盖。

锚点按违规类型不同，由判定核心随判定输出一并声明（§5.18 已定"判定输出含需要的证据切片区间"）：

| 违规 | 锚点 |
|---|---|
| 错步 | 错误动作被识别的那条观测 |
| 反序 | 打破顺序的那条观测 |
| 漏步（顺序型跳号） | 跳号被检出的那条观测 |
| 漏步（闭合时集合比对） | 实例闭合时刻 |
| 超时 | 时限越过的时刻 |

**判定核心声明锚点与必需跨度，前后余量是运行参数（数据）在 supervisor 侧叠加。** 核心不读配置（§5.18），故它只说"这段区间是这个结论必需的"；把余量加宽到人能看懂上下文，是 supervisor 用运行参数做的事。这样改余量不动核心，也不改判定逻辑的形状。

**必需跨度不可被余量配置压缩。** 若某结论的必需区间比配置的窗口长（例如超时违规的必需区间覆盖整个等待过程），取两者的并集，不截断。配置能加宽，不能削掉证据本身。

关键帧默认在锚点取一张。

#### 再切片：受滚动录像窗口约束的三个时段

再切片是**用新窗口从滚动录像重取**，因此能力边界就是滚动录像还在不在：

| 时段 | 能做什么 | 产物性质 |
|---|---|---|
| 锚点后 ≤ 原始素材窗口（默认 24 h） | 按任意新窗口重取 | 从未重编码的原始分段 `-c copy`，与首次切片同质 |
| 原始素材窗口 ~ 滚动窗口（默认 24 h ~ 7 天） | 按任意新窗口重取 | 从已归档压缩的分段取，画质为压缩后水平，**记录须标注素材代次** |
| 超过滚动窗口（默认 7 天） | **不能重取** | 只能复核当初已提取并上传的片段 |

**这条边界必须在界面上可见，不能让复核人点了才发现。** 每条证据显示"可再切片截止时刻"，过期后按钮置灰并说明原因。这是 §5.5 容量公式的直接后果，不是实现缺陷：滚动录像是有限窗口，延长它要么加盘要么开压缩（§5.19）。

**想让某些工位的再切片窗口更长，就延长该机的滚动录像窗口**——这正是 §5.19 里"压缩省下的容量换成可配置保留期"的用途所在。

#### 再切片的执行路径

中心不推、推理机拉（§5.3），故再切片是**委托命令**，不是同步调用：

1. 复核人在中心发起再切片，给出新的前后余量（锚点不可改——锚点是判定事实，改锚点等于换一个结论的证据）。
2. 请求进入待办命令队列，`evidence` 拥有请求与结果记录。
3. 目标推理机下次拉取时取到该命令，从本地滚动录像切片、上传，按幂等键回报。
4. 新片段**追加**为该判定的另一条证据，标注窗口参数、素材代次与发起人；**不覆盖原片段**。

**延迟是一个拉取周期，不是实时的。** 界面按异步任务呈现进度（§5.15 已定长任务用 `GET /api/v1/jobs/{id}` 轮询），不假装同步返回。

**为什么不覆盖原片段**：原片段是当初判定的依据，覆盖它就是改写证据链。多条并存、各自标注来源，复核结论指向具体哪一条。

**推理机离线时**：命令留在队列，恢复后执行；若届时素材已过滚动窗口，命令以"素材已过期"失败结案并明确告知，不静默丢弃。

#### 与既有决策的关系

- 不新增模块：`evidence` 已拥有"证据引用与人工复核"（§六），再切片请求与结果是它的记录；委托命令队列归 `device`（它已拥有推理机与委托执行这个 seam，ADR-0008 的"测试连接委托推理机执行"是同一机制）。
- 不改保留策略的类别语义（§5.19）：再切片产物属于原判定所在的保留类别。
- 不进实时路径：再切片是复核期行为，与 500 ms 预算无关；但它与压缩任务争同一套编解码资源，须同受限速与让路约束（§5.19 约束 3）。

---

## 六、系统结构

**【已定】技术与部署基线**：中心后台采用 FastAPI + SQLAlchemy 2.x + Alembic；基础 Web 采用 Vue 3 + TypeScript + Vite，并通过 OpenAPI 生成客户端。Nginx 是统一访问入口。Nginx、FastAPI、ARQ worker、PostgreSQL、MinIO、Redis 与原样复用的训练微服务由一份中心机 Compose 统一安装和运维。Web 登录使用 `HttpOnly + Secure + SameSite` 会话 Cookie，修改请求执行 CSRF 校验，不在 `localStorage` 保存长期令牌。

中心后台是**模块化单体**：业务模块在一个 FastAPI 部署单元内通过进程内接口协作，各自拥有行为、表和迁移；首版不引入内部 HTTP、服务网格、消息总线或分布式事务。

首切片持久化只启用 PostgreSQL、MinIO 与 Redis；TimescaleDB 在 `monitor` 开始持久化时序上报时的实施切片中再启用。推理机本地状态用 SQLite（§5.7）。

**【已定】统一访问入口是控制面入口，不是全流量中继。** Nginx 只承载 Web 静态资源、中心后台 REST/JSON、会话、小数据实时更新，以及原样复用的标注 UI 与训练微服务（在网关补鉴权）。视频的 WebRTC 信令与媒体均由浏览器直连推理机 mediamtx。

```
中心机（管理面，非运行面）                推理机 A（自治判定单元）      推理机 B
├── Nginx 统一访问入口                   ├── 推理服务容器             ├── 推理服务容器
│    ├── Vue3 前端                       │    DeepStream→DDM→vLLM     │
│    ├── /api/v1 → FastAPI               │    →SSE→supervisor 判定     │
│    └── 反代：标注 UI / 训练微服务       ├── supervisor（锁存/处置/    ├── supervisor
├── FastAPI 后台（单一入口点）            │    上报/对账/证据切片）      │
├── PostgreSQL（+Timescale 后置）         ├── 连接器运行时              ├── 连接器运行时
├── Redis / MinIO / ARQ worker           ├── mediamtx                 ├── mediamtx
└── 训练微服务（原样复用）+ metadata_db   └── SQLite 本地状态          └── SQLite
                                               ↑                          ↑
          ← 上报（幂等 upsert）                相机 1~8                  相机 9~16
          → 拉取（模板/配置，推理机主动）  （视频不跨中心；中心离线现场继续判定与处置）
```

**中心后台模块**（10 个）：

| 模块 | 拥有 |
|---|---|
| `auth` | 用户、角色、权限、会话 |
| `device` | 推理主机、工位、相机、推理后端、连接器配置、凭据配置状态标志、健康聚合、工位运行参数覆盖、委托命令队列 |
| `execution` | 工位物理执行权：归属、改绑握手、强制改绑的双人确认、物理执行租约与续期（§5.17） |
| `template` | Excel 导入/校验/配置生成/版本/发布/绑定/desired-reported 对账 |
| `dataset` | 训练数据集登记、用途检查、派生制品引用 |
| `monitor` | 推理机上报的镜像：SOP 实例、判定、流健康；看板状态 |
| `alert` | 违规归档、处置记录归档 |
| `evidence` | 证据引用、片段窗口与再切片请求、人工复核（§5.20） |
| `retention` | 数据保留策略、判定类别解析、变更影响估算与引用保护（§5.19） |
| `job` | 异步任务权威与投递（outbox；ADR-0004） |

**`retention` 拥有策略、不拥有数据**：它解析"某实例属于哪个保留类别、该类别留多久、何时压缩"，删除由数据拥有者在自己的表上执行（`evidence` 删证据、`monitor` 删记录、`alert` 删处置归档、边缘运行时裁 SQLite 与录像）。它独立成模块的理由是有一条跨模块不变量需要单一所有者：**保留策略不得删除仍被开放违规、在审复核或未结案实例引用的数据**——该约束横跨 `evidence` 与 `alert`，散在各模块无人能强制。反过来让它也持有数据就会变成一个跨界读写他人表的模块，违反所有权规则。

**`execution` 与 `device` 分开的理由**：`device` 的变更驱动是"现场装了什么设备"，是可逆的配置 CRUD；`execution` 的变更驱动是"哪台机有权驱动这个工位的执行器"，是带 TTL 的跨机安全状态机，且承载全系统唯一的双人确认操作（§5.17）。把它混进 `device`，一次相机改名和一次可能同时驱动两套物理执行器的改绑会共用同一个模块接口与权限面。它有自己的不变量——**任一工位在任一时刻最多一台推理机持有未过期的物理执行权**——这条要在模块内可强制，不能靠调用方自觉。

**上报接收面为什么不是一个模块**：`monitor`（上报镜像）、`alert`（违规与处置归档）、`evidence`（证据引用与复核）由同一个上报入口写入，但三者变更驱动不同：`monitor` 只随上报契约变，`alert` 随处置动作种类变，`evidence` 还有**人工复核**这个完全不同的行为主体与生命周期（证据不随录像过期删除，复核由人触发且不改写原判定）。合成一个模块会让"机器写入的镜像"与"人参与的复核流程"共用一个接口。代价是上报入口适配层要在同一请求级 UoW 内写三个模块——这是 ADR-0002 已经允许的组合，且该适配层只做分发，不含判定逻辑。

诊断日志与 correlation id 传播不是模块，是共享基础设施（§5.15）。

**中心不再有的东西**：`judgment` 模块（判定移入推理机的判定核心）、`runtime` 的 ingest 进程（不再订阅 SSE）、流租约表（ADR-0001 作废，§5.7；但工位物理执行权仍需管，见 §5.17）。中心的 `monitor` 是**上报镜像**，不是判定权威——它的行覆盖来自推理机的幂等 upsert。

`dataset` 是独立模块而非 `template` 的一部分：`CONTEXT.md` 对「训练数据集」的定义明确写了它不定义 SOP 模板，并把「SOP 模板」列为 _Avoid_ 项，合并二者会在代码层重新粘合术语层刻意拆开的概念。

**推理机侧组成**（`apps/edge-runtime/`）：判定核心（纯标准库、纯函数、无钟无 I/O，§5.18）、边界求解、本地状态（SQLite）、supervisor（SSE 消费与判定调用 / 请求看护 / 锁存 / 处置派发 / 上报对账 / 计时器持有）、连接器运行时、mediamtx 看护、证据切片、录像压缩归档任务（§5.19）。`vendor/` 内只留一处最小 hook 调用本包（§5.11）。

**依赖规则**：判定核心不依赖任何相机 SDK、推理框架或连接器实现，只消费归一化观测；中心各模块各自拥有数据表，不跨模块直接读写；前端只调后端用例，不承载判定规则。

**【已定】边界靠机械检查，不靠评审。** 每个中心模块一个 Python 包，包内 `api.py` 是唯一允许的跨模块导入目标；`import-linter` 契约在合并门禁中强制：判定核心不得导入任何适配器包、任意模块只能导入他人 `api`、`adapters/*` 不得被 domain 层导入、**`vendor/` 内的 hook 只能导入 `edge-runtime` 中指定的那一个健康事件入口模块**（§5.11）。最后一条使"补丁面只剩一处纯追加"从纪律变成可检查的事实——没有它，那处 hook 可以在后续修改中逐步长出对 `edge-runtime` 任意内部模块的依赖，而 `vendor/` 里的代码不受本仓库 lint 覆盖这一点会让它长期看不见。物理表名带模块前缀（§七），使迁移所有权可纯静态判断。

**【已定】跨模块用例共享一个请求级事务**（[ADR-0002](../adr/0002-request-scoped-unit-of-work.md)）：HTTP 适配层开启并提交单个 Unit of Work，模块门面只参与、不自行提交，`Session` 经请求作用域注入。代价是事务边界不再兜底模块边界，故上述机械检查是本决定的前提而非可选增强。

**【已定】迁移为单一线性 Alembic 历史**，单一 `versions/` 目录，文件名前缀标注所属模块（`0007_template_...`）；合并门禁检查一个迁移文件只触及其前缀模块拥有的表。

**【已定】工作区与工具链**：中心后台 Python 锚定 3.12，`.python-version` 与 `uv.lock` 提交，CI `uv sync --frozen`；根一份 `pyproject.toml` + uv workspace。lint/type 用 ruff + mypy strict。**`apps/edge-runtime/` 的判定核心只依赖标准库**（§5.11 硬规则），因其运行在 DeepStream 容器的 Python 环境中，版本不由我们选择。Web 侧 pnpm workspace 在 `control-web` 落地时创建。

**【已定】配置与秘密**：`pydantic-settings` 单一 `Settings`，环境变量为唯一来源，秘密经 `*_FILE` 路径变量读文件而非读值；启动强制校验无默认弱值，缺失即拒启，不降级为不加密。推理机本地凭据同规则（§5.12）。

**【已定】会话与口令**：会话记录由 `auth` 存于 PostgreSQL（重启不掉线，可强制下线），口令用 Argon2id。不用无状态签名 Cookie——无法即时吊销。

**【已定】基础 Web 栈内选型**：Element Plus + Pinia；OpenAPI 生成的 TS 客户端置于 `apps/control-web/src/api/generated` 并提交，CI 校验重新生成后工作区干净。`packages/` 需两个真实调用方才允许上移。

**【已定】API 前缀**：控制面路径固定 `/api/v1`，但该前缀不是版本轴，不会出现 v2（[ADR-0003](../adr/0003-api-v1-is-a-fixed-prefix.md)）。`openapi.json` 由 FastAPI 生成并导出到 `packages/contracts/`；契约检查必须让删除路径/字段、新增必填字段、收窄类型失败。推理机是可能非同步升级的客户端，靠启动时版本握手而非 URL 版本。

**契约清单**：**推理服务 OpenAI 兼容 API**（基座既定，我们是同机消费者）、**上报契约**（推理机→中心：`ReportedDecision` / `ReportedViolation` / `ReportedHealth`）、**拉取契约**（中心→推理机：`TemplateBundle` / `DeviceConfigBundle`）。后两者字段留到运行切片按真实数据确定（§5.15）。

实时结果路径固定为：

```text
相机主码流 → DeepStream → chunk → DDM → vLLM → 判定核心（序列比对+边界+有效性+三值）
            → 本机 supervisor：违规锁存 / 处置执行（含写输出点位）/ 证据切片
            → 上报中心 → 中心 SSE → Web 看板
```

判定与处置在虚线以上全程不跨机。中心 SSE 只服务看板，不在防错功能路径上。REST 不轮询替代该流。

---

## 七、数据模型要点

物理表名带拥有模块前缀，使迁移所有权检查可纯静态判断，且 grep 一个前缀即得该模块全部持久化面。

### 中心（PostgreSQL）

- **配置域**（`device`）：`device_inference_host`（物理推理机：主机标识、网络地址、mediamtx 地址、健康状态、时钟偏移、**录像滚动窗口时长与磁盘水位阈值**，§5.19）、`device_inference_backend`（**一份模板配置的进程端点**：外键 → host、base_url、承载的模板绑定、健康状态）、`device_station`（含运行参数覆盖状态与覆盖组，§5.3；保留策略覆盖状态与覆盖组，§5.19）、`device_camera`（主/子码流路径、凭据已配置标志、外键 → station 与 inference_backend）。
- **物理执行权域**（`execution`）：`execution_station_grant`（某工位的物理执行权当前归哪台推理机、租约到期时刻、续期时刻）、`execution_handover`（改绑握手请求与交权确认；强制改绑的两名确认人、警示已展示标志、确认时刻）。见 §5.17。
- **委托命令队列**（`device`）：`device_pending_command`（目标推理机、命令类型 = 测试连接 / 再切片、参数、幂等键、状态与回报结果）。中心不推、推理机拉（§5.3），故一切需要推理机代为执行的动作都经此队列；ADR-0008 的"测试连接委托推理机执行"与 §5.20 的再切片是同一机制的两个命令类型。
- **保留策略域**（`retention`）：`retention_policy`（全局默认与工位覆盖组：三个判定类别各自的证据保留时长、记录明细保留时长、压缩起始年龄、聚合归档年龄；覆盖为整组替换）、`retention_sweep`（每次回收任务的执行记录：范围、删除计数、跳过计数与跳过原因、起止时刻），使"上次删了什么、为什么跳过"可查。录像滚动窗口不在此表——它按推理机配置，属 `device_inference_host`（§5.19）。
- **连接器域**（`device`，仅配置）：`device_connector`（类型 = `hikvision_isapi` / `board_card`、连接参数、健康状态、**该适配器的能力声明**：投递方式、最大投递延迟、是否保序、是否可能丢边沿、时间戳来源，§5.8）、`device_connector_point`（方向 in/out、点位号、语义标签如"工件到位"/"停线联锁"、外键 → connector 与 station）。模板按语义标签引用点位，不写死设备地址。**工位可以没有任何连接器。**
- **模板域**（`template`）：`template_sop`、`template_version`（不可变，含 Excel 导入引用、actions.json、vlm_prompts、**运行参数默认值**、顺序性声明、sha256、发布者与发布时刻）、`template_draft`（可编辑，含 `revision` 乐观锁列）、`template_station_binding`（`desired_version` / `reported_version`，后者由推理机上报，§5.3）。
- **训练数据域**（`dataset`）：`dataset_training_dataset`、`dataset_member`（**数据集内的视频**：MinIO key、来源、大小、sha256、时长、编码）、`dataset_annotation`（动作时间段标注）、`dataset_usage_check`（DDM/VLM 用途、状态、原因）、`dataset_artifact`（生成的 DDM `annotation.json` 等派生制品及摘要）。上传、标注、用途检查、转换和训练是不同状态，不合并成一个"成功"。
- **上报镜像域**（`monitor`）：`monitor_sop_instance`（起止、闭合原因、边界信号来源、上报时刻）、`monitor_decision`（hypertable，判定结果 + 原因码 + 模板版本 + 推理机自报模型标识）、`monitor_observation`（hypertable，动作编号与外部信号同表）、`monitor_stream_health`（hypertable，含时间锚定偏移）。全部按事件 id 幂等 upsert，权威在推理机本地。
- **违规域**：`alert_violation`（kind、锁存标志、来源推理机）、`alert_disposal`（action、执行状态、幂等键）、`evidence_evidence`（MinIO key、类型、**锚点时刻、窗口前后余量、素材代次、发起来源=自动/再切片**）、`evidence_reclip_request`（新窗口参数、目标推理机、状态、发起人、失败原因如"素材已过期"）、`evidence_review`（复核结论、复核人、指向具体哪条证据）。违规与处置的执行权威在推理机，中心表是归档与复核载体。
- **基础域**（`auth`）：`auth_user`（含停用状态）、`auth_role`、`auth_permission`、`auth_role_permission`、`auth_session`。**不设 `auth_audit_log`**（§5.15）。
- **作业域**（`job`）：`job_application_job`（异步任务权威与 outbox；ADR-0004）。

**判定结果表归 `monitor` 而非独立的 judgment 模块**：判定核心是推理机内的纯函数，永久无表。中心侧只存上报镜像，命名与所有权都归 `monitor`，表前缀规则与纯函数要求因此不再互锁。

### 推理机（SQLite，每机一份）

`local_config`（已确认的设备配置与凭据密文）、`local_template_version`（已确认模板版本 + sha256）、`local_sop_instance`、`local_decision`、`local_violation`（锁存）、`local_disposal`（含幂等键与执行结果）、`local_report_queue`（待上报，至少一次）、`local_evidence_queue`（待上传）。

**关键不变量**：违规一经确认即锁存，不因后续补做或处置失败而消失；锁存发生在推理机本地，不依赖中心可达。

---

## 八、开发路线

与最初五阶段的**两处最大差异**：DDM 训练从阶段三提前到阶段一（没有 DDM 权重推理服务起不来，§2.7）；判定逻辑从中心移入推理机的 supervisor 进程（§5.11、ADR-0005），故基座改造与判定核心同属阶段 0。

### 阶段 0（4090 到位前，全部用真实数据可验证）

1. `git subtree` 接入 NVIDIA 仓库；根 `CONTEXT.md` + 首批 ADR（0005~0009）。
2. **基座契约测试 + 自有回归测试**（§5.9）——§2.2 的返工用例是第一个要通过的。
3. **判定核心**（`apps/edge-runtime/`，纯标准库纯函数）：序列比对、声明式边界、有效性门、三值判定、首批 14 个原因码及其未知码兜底约定、顺序型跳号即报。单元测试覆盖安全不变量。
4. **基座一处改造 + 最小 hook**（§5.11）：pipeline `on_message` 把流健康作为合成事件送进 `_vlm_response_queue`。维护为可重放补丁；纯 CPU 可验证的部分立即验证。基座 checker 与处置按配置关闭，不打补丁。
5. 中心数据模型与迁移。
6. 鉴权 + 自定义权限 + 诊断日志基线。
7. 设备配置 CRUD（推理主机 / 工位 / 相机 / 推理后端绑定）+ **工位运行参数两档切换**（跟随模板 / 工位自定义，§5.3）。
8. Excel 解析校验 + `actions.json` / `vlm_prompts.txt` 生成（用真实 Excel 验证，严配基座正则）。
9. 视频逐个上传（预签名直传 MinIO）+ 数据集登记 + **原样复用基座标注 UI 并在网关补鉴权** + DDM/VLM 用途检查 + 由标注生成 DDM `annotation.json`。
10. mediamtx 预览与录像 + 浏览器直连多路视频（**首验海康子码流 `has_b_frames`**，并验证目标大屏并发解码与网络容量，§5.5）。
11. 推理机 supervisor 骨架：本地状态、模板拉取与本地落盘、上报队列与对账、时间锚定、证据切片（真实 RTSP 验证断流检出与重连后重新锚定）。
12. **连接器抽象 + 海康 ISAPI 适配器**（真设备，非 mock）：读输入点位、写输出点位、**实测并写入能力声明**（投递方式、最大投递延迟、是否保序、是否可能丢边沿、时间戳来源，§5.8），以及模板绑定时按能力声明的角色校验。
13. 前端骨架：布局、路由、鉴权、设备与模板管理页。
14. 原样复用训练微服务接入（Nginx 反代 + 网关鉴权 + 数据库合并，Q35）。

除判定核心的集成验证外，其余均可运行、可测、可演示。

### 阶段 1（4090 到位）

15. 用本地官方数据训 DDM（单卡 resnet50，产出真实 checkpoint）。
16. 起推理服务：真实 DDM 权重 + 公开 `Cosmos-Reason2-2B`，含该处改造与 supervisor 内的判定核心，用真实视频打通 `actions.json` → DDM 分段 → VLM → SSE → 判定全链路。
17. 录制真实 chunk 流与流健康事件序列作为集成测试夹具（真实数据回放，非 mock）。
18. GPU 门禁：按 §5.6 两类起点分别计量至 Web 渲染；同时验证**每后端常驻显存开销**（§5.10）、DDM-only / VLM-only / 全链路显存、断流重连与 OOM 故障注入、**N 后端 × 1 路**真实并发、72h 稳定性。
19. **录像压缩实测**（§5.19）：在目标子码流上扫 CRF 定默认档、测 NVENC 与推理的实际争用、验证压缩任务限速与让路，据此复算 §5.5 容量表并确定可支持的保留期上限。
20. 中心不可达演练：拔中心网线，验证判定、处置、锁存、本地排队继续；恢复后对账不重复处置、不丢证据。

**退出门禁**：若单路 VLM 持续 backlog，立即降 VLM 为低频/旁路能力，重新评估产品承诺。

### 阶段 2

21. 完整 SOP 实例生命周期接真实流（含 IO 物理边界与空闲超时闭合）。
22. 违规记录 + 5 种处置动作 + 证据切片（锚点 + 可配置前后余量，§5.20）+ 人工复核留痕闭环。
23. 看板 SSE 与上报镜像（`monitor` 模块持久化启用 TimescaleDB，同时启用记录的原生压缩，§5.19）。
24. **数据管理**（§5.19、§5.20）：`retention` 模块策略配置与回收任务、三类判定的证据保留、变更影响估算与引用保护、磁盘水位告警；录像压缩归档任务（含与判定争用的实测门禁）；**再切片委托命令**与复核界面上的可再切片截止时刻。

### 阶段 3

25. 步骤对齐（时间码）+ 绑定对账与切换语义。
26. 多机部署 + **`execution` 模块的交权握手、强制改绑双人确认与物理执行租约**（§5.17）、**板卡连接器适配器**（产线级联锁，第二个适配器验证能力声明这层抽象成立）、时间同步监控、多路扩展验证。

### 阶段 4

27. 统计报表、整改闭环、商业化加固。

---

## 九、验收门禁

**正确性**：

- **返工序列不得误报**（§2.2 的 `1,2,3,2,4,5` 判通过）——这是本产品与基座原样行为的分水岭；
- 注入断流 / 丢帧 / 推理超时 / 时间戳跳变 / 点位断线 / 重连后时间轴归零时，系统不得输出错误的不通过；
- **闭合前有效性门生效**：断流期间触发空闲超时闭合时输出不可判定 + `STREAM_LOST`，不进入漏步集合比对；
- 漏步在实例闭合时立即报出（不等下一遍）；顺序型模板跳号即报；
- 每个不通过可追溯到模板版本、推理机自报模型标识、观测与证据；
- 模板切换期间旧实例不污染新版本；
- **未配置任何连接器的工位与配置了连接器的工位走同一套判定代码路径**（无 `if 配了连接器` 分支）；
- **工位运行参数生效值正确**：跟随模板的工位取当前绑定版本默认值，自定义的工位取自己那组且绑定新版本后不被默认值覆盖（§5.3）；
- **点位角色校验在绑定时生效**：把能力声明不足以承担该角色的点位声明为开始/结束信号时，绑定失败并给出具体原因，而不是在现场表现为不可判定（§5.8）；
- **未知原因码不致盲**：前端遇到映射表外的原因码时显示原始码 + 通用提示，其余信息照常显示，不空白、不丢弃该条判定（§5.2）。

**多机安全**（`execution`，§5.17）：任一工位在任一时刻最多一台推理机持有未过期物理执行权；交权握手未确认前新机不得写该工位输出点位；强制改绑必须记录两名确认人；租约过期后该机停止写点位且界面显式标注物理防错已失效。

**数据保留与压缩**（§5.19）：**所有保留时长与窗口均可配置，回收与切片代码路径无时间字面量**（机械检查保证）；配置校验拒绝非法值（0、负数、明细保留期短于压缩起始年龄、再切片期望超过录像滚动窗口）；"永久"与"未设置"在界面上可区分；三个判定类别各自的保留时长独立生效，挂有已锁存违规的实例按不通过类保留；缩短保留期只对将来生效且提交前显示影响估算；**仍被开放违规、在审复核或未结案实例引用的数据不被删除**（回收任务须报跳过计数与原因）；上报队列与证据队列不因空间回收而删除；磁盘水位越阈值时该路进不可判定并告警，不静默写失败。录像压缩须验证：**压缩任务运行期间判定链路的 p99 与超预算计数不劣化**（NVENC 与推理的实际争用在目标 GPU 上实测，不假设互不影响）；重编码产物校验通过后才删原分段，校验失败保留原文件并告警；**扫一组 CRF 定档**——取复核可用性开始劣化前的那一档为默认值，据此复算 §5.5 容量表；压缩后分段仍可支撑人工复核判断与再切片（由人判断，不用客观指标代替）；证据默认不重编码，其 sha256 与上传时一致。

**证据与复核**（§5.20）：片段窗口的必需跨度不被余量配置截断（取并集）；再切片不改锚点、追加不覆盖、标注窗口参数与素材代次；**界面显示可再切片截止时刻**，过期后按钮置灰并说明原因；素材已过滚动窗口的再切片命令以明确原因失败结案，不静默丢弃；归档压缩后的分段再切片产物**可在复核界面直接播放**（§5.19 约束 2）；复核结论指向具体哪一条证据。

**自治性**：切断中心—推理机网络后，判定、违规锁存、处置（含写输出点位）、证据切片、预览全部继续；恢复后上报对账幂等，处置不重复执行，证据不丢失；不兼容版本显式告警拒绝接入，不静默降级。

**性能**：按 §5.6 两类起点分别报告 p50/p95/p99、最大值和超预算计数；每个成功实时结果必须 ≤ 500 ms，测试窗口内超预算结果为零。拆分 DeepStream/chunk、DDM、VLM、判定核心、supervisor、上报、中心 SSE 和 Web 渲染耗时。长流 chunk delay 不持续增长；预览不得反压判定；**外部信号到判定的延迟**单独报（轮询模式下须报轮询周期）。**每推理后端的常驻显存开销单列**（§5.10 容量公式的一项）。

**稳定性**：先 72h 再 7 天；记录 RSS、显存、句柄、队列深度、延迟趋势、时间锚定偏移漂移、**SQLite 本地状态增长与录像磁盘水位**（§5.5 容量公式）；覆盖容器重启、宿主重启、磁盘不足、相机重启、连接器掉线恢复、中心离线与恢复。

**安全**：所有管理 API 认证授权（含反代的标注 UI 与训练微服务）；模板版本 sha256 校验且推理机上报实际生效值；推理服务离线启动不访问公网；**凭据不出现在中心的任何导出物、日志或 API 响应中**（§5.12）；**拉取响应按请求方主机裁剪**——以主机 A 的身份拉取，响应中不出现任何仅属于主机 B 的标识（§5.15）；**写输出点位的动作全部产生诊断日志事件**（物理联锁属高影响操作），并记录操作人与目标点位。
