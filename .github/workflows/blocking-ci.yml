name: blocking-ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: blocking-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  # Family 1 (harness §7): repository policy and lockfile cleanliness. Always runs, and
  # deliberately duplicates `make check`'s own first target: the gate below is path-filtered,
  # and a dependency change whose lockfile was never regenerated is exactly the change that
  # must not be able to reach main through a filtered-out path.
  lockfile:
    name: Lockfile cleanliness
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout candidate
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - name: Install the pinned Python toolchain runner
        run: pipx install uv==0.12.5
      - name: Require uv.lock to match pyproject.toml
        # `--check` resolves without writing. `uv sync --frozen` is not a substitute: it
        # declines to update the lockfile rather than comparing it to the manifest, so a
        # manifest edit with a stale lockfile installs the old resolution and passes.
        run: make lockfile
      - name: Set up the pinned Node runtime and package manager
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          # `.nvmrc`, the same file a developer's `nvm use` reads — one place the runtime
          # version is chosen, as `.python-version` is for the backend. `package.json`'s
          # `engines.node` is a floor rather than a pin and would let CI silently move to a
          # newer Node than anyone has run locally.
          node-version-file: .nvmrc
      - name: Install the pinned package manager
        # `corepack` reads `packageManager` from `package.json`, so the pnpm version comes from
        # the same manifest a developer's shell reads and not from a second pin here.
        run: corepack enable pnpm && corepack install
      - name: Require pnpm-lock.yaml to match the web manifests
        # The same rule as `uv.lock` above, for the web workspace: `--frozen-lockfile` fails on
        # a manifest whose lockfile was never regenerated. `--lockfile-only` stops before
        # writing any package to disk, since this job is about the lockfile and not about
        # having an installed tree.
        run: pnpm install --frozen-lockfile --lockfile-only

  # Family 2 (harness §7): the CPU-only merge gate, invoked exactly as a developer invokes
  # it. Path filtering is an optimization, not an exemption — when nothing relevant changed
  # this job still runs and returns an explicit success, so a skipped job can never be the
  # reason the required status is green.
  merge-gate:
    name: Repository policy, boundaries, and base contract
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout candidate
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Decide whether the gate has anything to check
        id: relevant
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        # `make check` reads the whole tree — it validates Markdown links and the repository
        # layout as well as code — so almost every path is relevant to it. Only agent
        # scratch space and per-agent local settings are not. `.mcp.json` is relevant: the
        # policy check reads it for a literal credential. Keep this list short; a broad
        # exclusion here is a check that stops running.
        run: |
          set -euo pipefail
          if [ -z "$BASE_SHA" ] || ! git cat-file -e "$BASE_SHA^{commit}" 2>/dev/null; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "No usable base commit; running the full gate."
            exit 0
          fi
          changed=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" \
            | grep -vE '^(\.scratch/|\.claude/)' || true)
          if [ -n "$changed" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Install the pinned Python toolchain runner
        if: steps.relevant.outputs.run == 'true'
        # `make check` starts by running `uv sync --frozen`, which provisions the interpreter
        # named by `.python-version` and installs every dependency and gate tool from
        # `uv.lock`. uv itself is all this step provides — no separate Python setup action,
        # which would be a second place the interpreter version is chosen. Its version is
        # frozen so CI and a developer run the same build of ruff and mypy.
        run: pipx install uv==0.12.5
      - name: Set up the pinned Node runtime
        if: steps.relevant.outputs.run == 'true'
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6.5.0
        with:
          node-version-file: .nvmrc
      - name: Install the pinned package manager
        id: pnpm
        if: steps.relevant.outputs.run == 'true'
        # `corepack` reads `packageManager` from `package.json`, so pnpm's version comes from
        # the manifest rather than from a second pin here. After the Node step, because the
        # shim it writes belongs to that interpreter. The store path is asked of pnpm rather
        # than hard-coded, so a release that moves it does not silently leave the next step
        # caching an empty directory.
        run: |
          set -euo pipefail
          corepack enable pnpm
          corepack install
          echo "store=$(pnpm store path)" >> "$GITHUB_OUTPUT"
      - name: Restore the pnpm store
        if: steps.relevant.outputs.run == 'true'
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          # Not `setup-node`'s own `cache: pnpm`, which resolves the store path before the step
          # above has put pnpm on PATH.
          path: ${{ steps.pnpm.outputs.store }}
          key: pnpm-store-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: pnpm-store-${{ runner.os }}-
      - name: Run the local merge gate
        if: steps.relevant.outputs.run == 'true'
        run: make check
      - name: Hold the change to its size budget
        if: steps.relevant.outputs.run == 'true' && github.event_name == 'pull_request'
        # Harness §5's budget is per landed stage, so it is measured on the pull request's
        # own diff — a push to main has no single base to measure against. Pull requests
        # only, therefore, while `make check` above runs on both.
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: make change-size BASE="$BASE_SHA" HEAD="$HEAD_SHA"
      - name: Verify a clean worktree
        if: steps.relevant.outputs.run == 'true'
        # Generated output is regenerated in the same change as its source (harness §5). If
        # the gate wrote anything — a re-resolved lockfile, a formatter fixing a file — the
        # change is incomplete.
        run: git diff --exit-code
      - name: Explicit success with nothing to check
        if: steps.relevant.outputs.run != 'true'
        run: echo "No paths the merge gate reads were changed."

  # Family 3 (harness §7): the center backend against real containerized infrastructure. A
  # separate job because it needs Docker, which `make check` deliberately does not. Same
  # explicit-success rule as above — path filtering is an optimization, not an exemption.
  integration-gate:
    name: Center backend against real PostgreSQL
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout candidate
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Decide whether the suite has anything to check
        id: relevant
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        # Narrower than the merge gate's filter: this suite reads the center backend, its
        # migrations, the lockfile it installs from, and the target that invokes it.
        run: |
          set -euo pipefail
          if [ -z "$BASE_SHA" ] || ! git cat-file -e "$BASE_SHA^{commit}" 2>/dev/null; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            echo "No usable base commit; running the suite."
            exit 0
          fi
          changed=$(git diff --name-only "$BASE_SHA" "$HEAD_SHA" \
            | grep -E '^(apps/control-api/|Makefile$|uv\.lock$|pyproject\.toml$|\.github/workflows/)' || true)
          if [ -n "$changed" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Install the pinned Python toolchain runner
        if: steps.relevant.outputs.run == 'true'
        run: pipx install uv==0.12.5
      - name: Run the integration suite
        if: steps.relevant.outputs.run == 'true'
        # Docker is present on the GitHub-hosted runner, which is what testcontainers uses to
        # start PostgreSQL. No service container is declared here: the suite owns the image
        # tag and the lifecycle, so a developer runs the same database CI does.
        run: make check-integration
      - name: Explicit success with nothing to check
        if: steps.relevant.outputs.run != 'true'
        run: echo "No paths the integration suite reads were changed."

  required:
    name: CI required
    if: ${{ always() }}
    needs: [lockfile, merge-gate, integration-gate]
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Require successful dependencies
        env:
          LOCKFILE_RESULT: ${{ needs.lockfile.result }}
          GATE_RESULT: ${{ needs.merge-gate.result }}
          INTEGRATION_RESULT: ${{ needs.integration-gate.result }}
        run: |
          set -euo pipefail
          status=0
          for pair in "lockfile:$LOCKFILE_RESULT" "merge-gate:$GATE_RESULT" \
            "integration-gate:$INTEGRATION_RESULT"; do
            name=${pair%%:*}
            result=${pair#*:}
            echo "$name: $result"
            test "$result" = success || status=1
          done
          exit $status
