{
  "name": "nvsop",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.23.2"
  },
  "scripts": {
    "web:format": "pnpm --filter control-web run format",
    "web:lint": "pnpm --filter control-web run lint",
    "web:typecheck": "pnpm --filter control-web run typecheck",
    "web:test": "pnpm --filter control-web run test",
    "web:build": "pnpm --filter control-web run build"
  }
}
