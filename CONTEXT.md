# SOP 合规检测系统

面向工厂交付的视觉 SOP 执行与合规产品：把工厂定义的标准作业转成机器可执行的模板，在现场持续回答当前在做什么、是否合规、偏离后留什么证据、触发什么响应。本文件只定义领域语言，不含实现细节。

## Language

### 子系统

**推理服务**：
消费视频、产出动作编号序列并完成合规判定的服务。它是本系统的组件，不是外部依赖；其感知计算路径不被修改。
_Avoid_: 推理引擎、算法层、DeepStream、模型服务

**中心后台**：
承载配置、模板、上报镜像、告警归档与证据的服务端。它不直接接触视频，也不在实时判定路径上。
_Avoid_: 服务器、主控、平台

**推理机**：
运行推理服务的物理机，是一个自治判定单元：本地完成取流、感知、判定、违规锁存、处置与证据缓冲，中心不可达时继续工作。
_Avoid_: 边缘节点、GPU 机、算力池

**边缘运行时**：
推理机上的自研组件，掌握本机配置与凭据、模板版本落地、违规锁存、处置派发、证据切片与向中心上报对账。
_Avoid_: 探针、采集器、客户端、边缘 agent

### 工位与设备

**工位**：
独立运行一个 SOP 实例的最小作业空间。判定与违规都挂在工位上，不挂在相机或人员上。
_Avoid_: station、机位、岗位、产线节点

**推理后端**：
承载一份模板配置的推理服务进程端点。一台推理机可运行多个推理后端；共用同一后端的相机必须共用同一模板。
_Avoid_: 推理节点、GPU 机、算力池

**空闲时限**：
工位在无新观测后判定当前 SOP 实例已结束所需的静默时长。由模板声明，现场可调，按推理机主机单调钟度量。
_Avoid_: 超时、timeout、心跳间隔

**停用**：
可变配置对象（工位、相机、推理后端、连接器、用户）不再参与新的绑定、登录或运行，而其历史记录与关联关系保持可查的状态。停用可逆。它不是删除：删除是另一项按权限开放的操作。
_Avoid_: 禁用、下线、失效、软删除

### 模板与执行

**SOP 模板**：
声明式的工序合规定义，由通用判定逻辑解释执行；不生成代码，不为单个场景硬编码。
_Avoid_: 方案、配置、规则文件、流程

**模板版本**：
由具备模板编辑权限的用户发布后，可绑定到工位执行的一份不可变模板记录，锁定步骤、运行参数与推理服务所需配置。发布后不可修改；回滚即重新绑定某个历史版本。
_Avoid_: 发布物、快照、release 包

**步骤**：
SOP 模板中的一个作业单元，含名称与描述。
_Avoid_: 工步、作业内容、环节

**SOP 实例**：
工位上的一次实际作业周期，有明确起止边界。同一工位同时最多有一个活动实例。起止边界由模板声明的信号满足与否界定，不由代码分支决定，也不由动作序列形状推断。
_Avoid_: 一遍、一次、批次、周期、cycle

**规范化 SOP 文档**：
平台规定格式的 Excel 工作簿，含工位表与步骤表，是创建模板草稿的导入来源。平台保留原文件和导入记录供追溯；导入后不再作为模板的权威记录。
_Avoid_: SOP 文件、工艺文件、需求表

**模板草稿**：
平台内可编辑的 SOP 模板权威记录，可由规范化 SOP 文档导入后继续修改；尚未发布，不能绑定到工位。
_Avoid_: 未发布模板、测试模板

**训练数据集**：
为训练某类感知模型而成组登记的一批视频及其标注。视频逐个上传，不以压缩包整体导入；系统分别记录各模型用途的可用性。训练数据集不定义 SOP 模板，也不因导入或转换成功而表示训练成功。
_Avoid_: SOP 模板、训练包、样本文件夹

**动作时间段标注**：
在一段完整视频的时间轴上标出各动作的起止时刻，是产生 DDM 训练所需片段边界的唯一途径。标注由平台内的功能完成，不依赖外部工具。
_Avoid_: 打标、切片、划片段

### 判定

**观测**：
带时间信息的客观事实陈述，不含业务结论。动作编号、外部信号都是观测；"这一步完成了""这次不合规"不是观测。取流状态不是观测，见流健康事件。
_Avoid_: 检测结果、识别结果、事件

**观测有效性**：
系统在一段时间内是否具备可靠观察能力。持续有效且未观察到目标，才可判定目标未发生；断流、推理不可达、时间未对齐均表示不可判定，不能解释为未发生。
_Avoid_: 置信度、可信度、健康度

**流健康事件**：
推理服务对某一路取流状态的陈述，如源错误、正在重连、重连成功、最后一帧时刻、恢复后时间轴归零。它是**观测有效性的输入，不是观测**：观测陈述作业发生了什么，流健康事件陈述我们当时能否可靠观察。因此它不参与漏步集合比对，也不进入观测的存储与上报路径。它只在推理服务进程与取流管线存活期间产生；进程级失联不由它表达，由观测的持续静默表达。
_Avoid_: 探活结果、心跳、流告警、掉线事件、健康度

**判定结果**：
SOP 实例的业务结论，只有通过、不通过、不可判定三值。不可判定表示无法可靠得出结论，不等同于不通过。
_Avoid_: 检测结论、合规状态、成绩

**原因码**：
判定结果附带的机器可读理由，说明为何得出该结论。每个原因码在界面映射一句人类可读的排障提示。
_Avoid_: 错误码、状态码、error

**违规**：
已被确认的不合规事实，分为错步、漏步、反序、超时。违规一经确认即锁存，不因后续补做或处置失败而消失。
_Avoid_: 异常、报警、缺陷

**处置**：
违规发生后采取的响应，与违规事实分离。处置失败不改变已确认的违规事实。
_Avoid_: 动作、响应策略、联动

### 集成

**连接器**：
与外部设备或系统交互的可配置集成点。是否配置、配置几个，由部署现场决定；判定逻辑不因某个工位有无某类连接器而改变代码路径。
_Avoid_: 集成、插件、外部接口、IO 模块

**点位**：
连接器上一个可读或可写的具体信号位置，带方向与语义标签。模板按语义标签引用点位，不写死设备地址。
_Avoid_: 通道、端口、地址、IO 口

**外部信号**：
经连接器输入的观测。它与动作编号同为观测，在判定中无特殊地位。
_Avoid_: IO 信号、硬件信号、传感器数据

### 证据与复核

**证据**：
支撑一次判定的视频片段与关键帧，在推理机切片后上传中心后台，不随录像保留期过期而删除。
_Avoid_: 录像、截图、附件

**复核**：
人工对判定结果的确认或否定，留痕可追溯。复核不改写原判定，另存结论。
_Avoid_: 审核、申诉、复查
