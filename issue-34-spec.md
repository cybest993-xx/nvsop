Part of #1 · 原实施票 #34 · 前置 #25（已关闭）

## Problem Statement

运行操作员需要在中心 Web 查看工位现场的多路子码流，并按时间回看推理机已经录下的视频。目前工位、相机及所属推理机的配置已存在，但没有可运行的 MediaMTX 预览、分段录像和浏览器回看链路。仅保存媒体地址不能证明视频可播放，预览失败也不能说明 SOP 不通过。

视频经过中心中继会增加中心带宽负担；与推理服务的感知计算路径共用预览、编码或录像，会把观看者和播放器故障带入实时判定。另一方面，只有打开页面才录像，无法为无人观看期间的作业保留连续素材。

## Solution

在现有“工位与设备”入口提供相机媒体配置、多路预览、单路详看和按时间回看。浏览器依据已登记的相机与推理机关系，直接访问该推理机的 MediaMTX；中心后台只处理授权、非秘密配置和媒体描述。

每路相机显式选择零转码或 CPU 转码路径。仅预览时按观看需求拉流；需要连续录像的相机在无人观看时也持续录制。录像窗口取所属推理机的配置。画面是否真正出现、连接中断、重试和录像缺口都有明确提示，所有操作可用键盘完成。

本票交付真实可运行的软件链路及必要测试。具体相机编码、B 帧情况、目标 GPU 争用、并发容量和完整现场验证，继续由既有关联验证票承载。

## User Stories

1. 作为运行操作员，我希望从工位找到相机及其所属推理机，以便确认正在查看的现场。
2. 作为运行操作员，我希望同时选择多路相机预览，以便观察多个工位。
3. 作为运行操作员，我希望从多路预览进入单路详看并返回，以便查看细节后继续观察其他工位。
4. 作为运行操作员，我希望视频直接来自所属推理机，以便中心带宽和可达性不成为视频播放的前提。
5. 作为设备管理员，我希望逐路选择零转码或转码，以便适配实际相机与浏览器的编码组合。
6. 作为设备管理员，我希望转码路径标明额外 CPU 开销，以便判断现场资源是否足够。
7. 作为设备管理员，我希望未实测的编码与容量保持“未验证”，以便不把配置保存成功误认为部署验证通过。
8. 作为设备管理员，我希望离线保存媒体配置，以便在相机或推理机暂时不可达时准备部署。
9. 作为设备管理员，我希望在推理机本地配置相机凭据，以便中心、浏览器和导出物都不持有密码。
10. 作为设备管理员，我希望按推理机配置录像滚动窗口，以便根据本机磁盘容量调整保存时长。
11. 作为设备管理员，我希望非法媒体地址、路径、模式或保留时长被明确拒绝，以便在配置生效前发现错误。
12. 作为设备管理员，我希望并发编辑被识别，以便另一位管理员的配置不会被静默覆盖。
13. 作为运行操作员，我希望仅预览的相机在无人观看后释放取流资源，以便避免无需求的网络与 CPU 消耗。
14. 作为运行操作员，我希望开启连续录像的相机在首次打开页面前、关闭所有页面后仍然录像，以便无人观看期间也有素材可回看。
15. 作为运行操作员，我希望分别看到未连接、连接中、已出画面、首帧失败、断流和恢复状态，以便采取合适的排查或重试操作。
16. 作为运行操作员，我希望一路失败不影响其他路，以便仍能观察正常工位。
17. 作为运行操作员，我希望停止预览、切换相机或离开页面后释放旧连接，以便不会留下后台播放和重复重试。
18. 作为运行操作员，我希望按相机和时间查询实际录像区间，以便找到需要回看的作业。
19. 作为运行操作员，我希望在浏览器播放保留窗口内的录像，以便无需登录推理机查找文件。
20. 作为运行操作员，我希望未录像、区间缺口、素材过期和媒体访问失败有不同提示，以便不会把不存在的视频当作连续录像。
21. 作为运行操作员，我希望回看中的时间按 Asia/Shanghai 展示，以便与现场记录对照。
22. 作为运行操作员，我希望推理机或 MediaMTX 重启后仍能回看保留窗口内的已完成分段，以便重启不破坏历史素材。
23. 作为运行操作员，我希望中心暂时不可达时，已经建立的直连预览及推理机本地录像继续工作，以便管理面故障不扩大为现场盲区。
24. 作为运行操作员，我希望播放错误与 SOP 判定结果分别呈现，以便媒体故障不会被误认为违规或不通过。
25. 作为只读用户，我希望获授权后可查看媒体而不能修改配置，以便按职责使用系统。
26. 作为键盘用户，我希望选择相机、播放、停止、重试和回看都有标签及可见焦点，以便完成全部操作。
27. 作为用户，我希望错误和状态同时通过文字表达，以便不依赖颜色理解现场情况。
28. 作为部署维护者，我希望用合成 RTSP 视频在无 GPU 环境运行媒体链路，以便在真机到位前验证软件契约。
29. 作为部署维护者，我希望本机配置应用失败时保留上一份可用配置并报告原因，以便不会把一处错误扩散到其他相机。
30. 作为发布验证人员，我希望软件证据与相机、浏览器、目标网络和容量证据分别记录，以便明确哪些能力已经验证、哪些仍待现场确认。

## Implementation Decisions

1. **沿用既有 Module 所有权。** `device` 拥有推理机媒体地址、相机模式和录像窗口等配置、校验、授权及迁移；边缘运行时拥有本机媒体配置应用与进程看护；MediaMTX 提供取流、分发、分段录像、保留和回放。Web 功能归“工位与设备”。不新建中心媒体 Module、录像索引数据库或第二套录制 Implementation。

2. **复用现状，补齐媒体所需字段。** 复用推理机已有的 `mediamtx_address`、`recording_window_seconds`，以及相机已有的地址、主／子码流路径、工位与推理机关系。`mediamtx_address` 明确表示浏览器可达的 WebRTC 信令基地址；为原生回放监听地址提供独立的非秘密配置。浏览器不得从 WebRTC 地址猜测回放端口。每路相机新增显式的媒体路径模式与录像模式，枚举使用稳定的 snake_case 在线值。旧记录以零转码、连续录像作为配置默认值，媒体地址缺失仍表示未配置，不表示已运行。

3. **只传递调用方需要的媒体描述。** `device` 的查询用例从自己拥有的拓扑生成相机标识、显示名称、必要的工位信息、所属推理机、稳定的媒体 path、直连地址和配置模式。Web 不自行关联全厂拓扑、拼相机 RTSP URL 或决定拉哪台推理机。path 根据稳定相机标识确定，不随显示名称变化，不接受任意文件系统路径。中心不代理信令、视频、回放文件或 MediaMTX 管理请求。

4. **权限在用例层成立。** 媒体描述查询使用 `device.camera.view`，仅返回播放所必需的信息；相机媒体模式编辑使用 `device.camera.edit`，推理机地址及录像窗口编辑使用 `device.inference_host.edit`。按主机导出非秘密媒体配置需要对应的推理机和相机查看权限。Web 入口及可见操作使用同一权限约定，只有相机查看权限的用户也能到达媒体页。已有的会话、CSRF、`revision`／`If-Match` 和字段错误契约继续适用。

5. **补全本票可运行的本机配置入口。** 按指定推理机生成只含其相机与媒体配置的非秘密配置，由部署人员在该推理机本地应用，并从只读 secret 文件取得相机凭据。复用边缘运行时已有的本地配置校验与生产启动方式；媒体配置应用是实际启动路径，不是演示生成器。应能完成“中心登记／导出 → 本机校验和应用 → MediaMTX → Web 直连”的操作闭环。自动拉取、跨版本确认及完整 `DeviceConfigBundle` 仍由 #44 负责；本票不复制其拉取循环。中心保存成功与本机已应用分别表达，在没有回报证据时不得显示“已生效”。

6. **本机应用要可恢复。** 校验目标主机、相机标识唯一性、路径合法性、配置模式、地址、时长与本机秘密引用后再应用。重复应用同一配置不创建重复进程或媒体 path。候选配置失败时保留上一份可用配置，错误明确到受影响相机；启动和恢复不要求中心在线。导出及本机应用显式处理推理机、工位和相机的停用状态，不能假定主机停用会级联改变子对象状态。停用配置应用后停止对应取流与新录制，重启不得重新启用；保留已有录像的回放所需 path 与元数据，不因停用删除分段或使历史回看失效。

7. **零转码与转码只作用于子码流。** 零转码时 MediaMTX 拉取子码流并复用其编码。确需转码时，仅为该路配置独立的 CPU H.264 编码路径，产出供 MediaMTX 分发与录制的同一份媒体流。不得调用推理服务的预览输出或编码能力，不修改感知计算路径，不依赖 GPU。转码启动、退出、失败及资源限制归本机媒体看护；不为每位观看者重复创建转码进程。

8. **按需预览与连续录像的语义分开。** 录像模式为“仅预览”或“连续录像”，默认连续录像。“仅预览”不录制，利用 MediaMTX 的按需源能力，在最后一位观看者离开且等待期届满后释放源；它仅用于能够确认未参与本机 SOP 执行的调试相机，不能作为生产运行的录像保障。配置应用与 SOP 启动入口共同拒绝“相机参与 SOP 执行但仅预览”的组合，无法确认未参与时也拒绝该切换；该校验在运行入口完成，不改变纯判定核心。“连续录像”在本机启用该相机时即持续拉流与录像，不以浏览器连接数为开关；优先使用 MediaMTX 原生持续源配置，不制造用于维持连接的假观看者。两种模式共用同一媒体管理 Implementation，每路仅有一条实际来源链。

9. **不能把 `sourceOnDemand` 与 `record` 同时开启视为连续录像。** 本次核对的 MediaMTX 行为是按外部 reader 启停源，录像开关自身不会触发无人观看时的拉源。原 AC 中“按需拉流”由仅预览模式满足，连续录像按持续录像需求运行。实施时同步澄清既有机制说明中的这处配置歧义，并保留原 AC 追溯；不改动判定或自治性 ADR。

10. **录像由 MediaMTX 持有。** 使用其原生分段录制、持久化目录、回放和保留机制；回放选用所采用版本支持的录制格式。录像滚动窗口按推理机统一生效，使用已有配置值，默认 7 天只是配置默认值。分段长度及录制相关时长也从配置解析，不在路径中复制字面量。禁止 0、负数和不受支持的值；不能把 0 意外解释为无限保留。中心可离线保存待应用值，但缩短在用窗口的生效提交前，本机必须依据实际分段展示受影响对象数、空间及最早／最晚时间，取得对此次变更的明确确认；基准配置或目标窗口改变后需重新估算和确认。估算失败、无法取得真实影响或用户取消时保持原有效窗口，不能先更新 MediaMTX 再补确认。配置保存及确认均不做同步批量删除；新值只作用于之后的正常回收周期，执行记录包含变更前后值、操作人及影响估算。清理不能越过录像目录处理本地状态、上报队列或证据队列。

11. **回看直接使用 MediaMTX 的 Interface。** 浏览器按相机 path 和有效 UTC 时间区间查询实际可用录像，再向同一推理机请求选定区间的视频。区间须有合法起止关系；超窗、无录像、录像缺口、查询失败和播放失败分别表达。以实际可用区间为准，不把滚动窗口当作录像始终完整的保证，不跨缺口假装连续播放。网络时间使用 UTC RFC3339，Web 按 Asia/Shanghai 显示。回看是查看滚动录像，不生成证据，不覆盖未来的证据与复核流程。

12. **Web 媒体 Module 要有 Depth。** 页面通过一处 Interface 传入媒体描述和用户选择；Module 内拥有建连、首帧判断、失败重试、回放查询、播放及释放的完整生命周期。多路格子和单路详看复用该 Implementation。WebRTC 协商成功或收到 track 不等于首帧成功，必须以可解码的视频帧作为“已出画面”的依据。连接与首帧等待都有终止条件，自动重试有退避和上限，并提供手动重试。

13. **每路状态独立且资源有终点。** 关闭一路、切换媒体、离开页面、已知会话失效或权限撤销后，停止该路连接、待决请求、重试与回放资源；迟到的响应不能复活旧播放器。普通中心网络故障不当成权限撤销，也不主动停止已经建立的直连播放。浏览器自动播放被阻止时显示可操作的开始播放提示。媒体状态描述本次播放，不能写入判定结果、生成流健康事件或把设备标成已经通过实测。

14. **直连的网络条件必须可部署。** 使用推理机浏览器可达地址，明确 WebRTC 信令、ICE 媒体端口及回放监听的配置。HTTPS Web 配套浏览器信任的推理机 HTTPS 信令／回放，不能以关闭证书校验或降级混合内容解决。CORS 限定部署的 Web origin；本地网络可直达时不依赖公网 STUN/TURN。只开放观看所需能力，MediaMTX 管理和发布入口留在受限的本机或部署网络。

15. **保持已定的可信工厂网络模型。** 中心配置操作和产品入口受会话及权限控制；直连媒体依赖可信工厂网络／VLAN 与端口访问控制。首版不引入逐观看者 JWT、JWKS 或中心鉴权回调，不宣称中心 RBAC 能吊销同网段绕过 Web 发起的媒体连接。浏览器访问媒体时不转发中心会话凭证；拒绝媒体响应中跳到未配置主机的地址，避免把第三方返回 URL 当作任意导航目标。

16. **秘密只在本机使用。** 中心字段和导出物拒绝 userinfo、隐藏在查询串等位置的设备凭据；日志、错误和浏览器地址不得回显相机密码。需要含秘密的 MediaMTX 运行配置只能在本机受限位置生成，不能进入 Git、中心或导出物。路径与启动参数需校验，不能把配置直接当 shell 命令执行。

17. **预览与判定互不形成等待关系。** 媒体进程、缓冲与失败处理独立于判定输入和持久化路径；浏览器慢读、断开或转码失败不能阻塞判定，也不能无限积压内存。多路选择不写死固定容量；必要的部署资源限制来自配置，界面说明实际限制。资源争用及目标延迟仍须用目标环境证据证明。

18. **兼容性与部署证据随实现交付。** 新增字段及迁移保留旧配置、停用状态和历史关联；控制面兼容性及生成客户端随源契约更新。MediaMTX 和所需编码工具使用记录的版本及镜像摘要，提供无 GPU 的可重复启动入口与合成视频示例。当前仓库尚无这条媒体部署链，不能写成已有配置可直接启用。不上线替身、不以空响应或假状态完成闭环。

## Testing Decisions

**主要验收 seam 是“工位与设备”的 Web 媒体入口。** 从相机选择、模式配置、开始／停止／重试和录像查询这些公开行为验证用户结果。配置规则通过 `device` 的授权用例 Interface 验证；本机媒体行为通过真实 MediaMTX 的公开协议验证。这是同一功能在其拥有者处的必要证据，不为 HTTP handler、配置模板和每个私有辅助函数另造 seam。

- **好测试验证外部行为。** 断言真实媒体目的地址、可播放帧、实际录像区间、可观察的失败、资源释放与保留结果；不只比较配置文本，不断言私有调用顺序，不靠一串内部 mock 证明录制成功。
- **配置与持久化。** 覆盖 `device` 用例的权限、校验和冲突；新增列、默认值、迁移和事务使用真实 PostgreSQL。复用已有推理机、相机管理的用例测试及真实数据库夹具。已有数据升级后，未配置媒体的相机仍然清楚显示未配置。
- **媒体 Adapter。** 在 CPU 环境启动固定版本 MediaMTX，以合成 RTSP 流作为相机输入，验证零转码、真实 CPU 转码、按需启停、无人观看持续录制、断源恢复、进程重启后的已有录像和回放。测试替身只能放在相机输入或浏览器网络／媒体能力这样的已记录 seam；必要的协议测试必须真的连接 MediaMTX。
- **浏览器行为。** 复用现有设备页异步状态测试、直传媒体时不携带中心凭证的测试模式，以及现有 Playwright／快照检查。至少有针对真实媒体链路的首帧及回看冒烟；故障与迟到事件可在浏览器原生 Adapter seam 用确定性替身覆盖。覆盖 Chrome/Edge、键盘、焦点、非纯颜色状态、1366×768 与 1920×1080 的关键布局。
- **自治与隔离。** 用最小可重复场景证明中心不可达不停止已配置的本机录制，重启不要求中心在线；慢读／断连不阻塞独立判定驱动。替身不能用来宣称目标 GPU、真实相机、现场网络或容量已通过。
- **先验与门禁。** 实施遵循 TDD：先写最小失败场景，再实现对应行为。保留现有 `make check`、`make check-integration`、契约和 CI 浏览器门禁；新增真实 MediaMTX 的必要检查接入相应既有门禁，CPU 快速门禁仍无需 Docker。契约变更执行 `make contracts`。纯 spec 发布本身只做文档与追溯检查，不冒充实现测试。

实施时以同名场景 ID 关联自动化证据；本表全部是 #34 的软件验收，不能整体推迟到现场验证。

| 场景 ID | 前置与动作 | 可观察结果 |
| --- | --- | --- |
| SYS-34-01 | 相机／推理机离线；授权用户保存媒体配置，再提交非法地址、模式、窗口或过期 revision | 有效配置可保存；无虚假在线结论；非法输入返回字段错误，并发更新不丢失 |
| SYS-34-02 | 两台推理机各有相机；查询媒体描述并按主机导出 | 每路指向自己的推理机，导出只含目标主机；无相机秘密、任意文件路径或推测端口 |
| SYS-34-03 | 合成 RTSP 输入分别配置零转码和 CPU 转码；打开预览及回看 | 两条配置路径均产生真实可播放视频；媒体直接来自推理机，判定主码流不改动 |
| SYS-34-04 | 仅预览模式；从无观看者到有观看者，再关闭全部播放器 | 需求触发拉源；最后一位离开且等待期届满后释放；重复开关不遗留转码进程 |
| SYS-34-05 | 连续录像模式；启动后不打开页面，再打开并关闭全部预览 | 首次观看之前及全部退出之后仍有新分段，可按时间回看 |
| SYS-34-06 | 分别应用两个不同的合法录像窗口；缩短时分别取消、确认、令估算失败或变更基准配置 | 本机先展示真实影响；取消、估算失败及过期确认均保持旧窗口；有效确认后只影响后续正常清理，不同步批量删除，不使用固定 7 天，不删除录像目录外的数据 |
| SYS-34-07 | 已有多个录像区间及缺口；按 UTC 时间查询、播放，随后请求过期区间 | 回看实际可用区间，时间显示正确；缺口、无录像和过期不伪装成连续画面 |
| SYS-34-08 | 信令成功但没有可解码帧；随后恢复输入或手动重试 | 先显示首帧失败，真正出帧后才显示播放中；无无限等待或重复会话 |
| SYS-34-09 | 多路播放中中断一路并恢复；另一路保持输入 | 失败与恢复只作用于对应一路，其他路继续播放；不产生 SOP 不通过或流健康事件 |
| SYS-34-10 | 连接或回看请求未完成时切换、停止、离开页面，随后旧响应到达 | 无旧画面复活，无持续重试，连接及请求资源被释放 |
| SYS-34-11 | 推理机已有可用配置和录像；切断中心连接，再重启 MediaMTX／媒体运行入口 | 本机不因中心离线停止工作；已有分段仍能回看；不承诺尚未加载的中心 Web 能离线启动 |
| SYS-34-12 | 匿名、只有查看权限、只有编辑权限的用户，及已知会话失效／权限撤销 | 用例与 Web 权限一致；只读不能编辑；拒绝时停止对应界面操作；中心凭证不发给媒体地址 |
| SYS-34-13 | HTTPS 页面直连推理机，另注入错误证书、错误 origin 或外部跳转地址 | 正常部署可出帧与回看；不安全地址或浏览器限制有明确失败，不绕中心中继或降低校验 |
| SYS-34-14 | 本机配置含错误主机、缺失秘密、非法路径或无法启动的转码；再次应用原配置 | 坏配置不冒充生效，旧可用配置可继续或恢复；无重复 path、密码泄露或命令注入 |
| SYS-34-15 | 仅使用键盘在两个基准视口选择多路、详看、停止、重试和回看 | 操作可完成，焦点可见，控件有名称；错误和状态有文字，现有快照／浏览器测试保留 |
| SYS-34-16 | 媒体慢读、断开或转码失败，同时驱动已有判定入口 | 判定处理不等待播放器或媒体错误处理；硬件延迟与容量仍标记待验证 |
| SYS-34-17 | 子对象仍为 active 时停用其推理机，另分别停用工位或相机；应用配置并重启 | 对应取流与新录制停止且不被重启恢复，历史录像仍可查询回看，恢复须显式应用有效配置 |
| SYS-34-18 | 对参与 SOP 执行的相机请求仅预览，或为仅预览相机启动 SOP 执行；另测试未参与执行的调试相机 | 前两种组合明确拒绝，不能削弱连续素材保障；确认未参与执行的调试相机可按需预览且清楚标为不录像 |

## Out of Scope

- #44 的完整按主机拉取、模板完整性和原子确认协议；本票只交付媒体配置本地应用及真实直连链路。
- 实时运行看板、判定输入、违规锁存和处置；多路浏览不扩展成新的大屏布局管理产品。
- #52／#53 的证据切片、上传、再切片和人工复核；滚动录像回看不冒充证据。
- #55–#57 的分类保留策略、跨 Module 引用保护、通用策略影响估算工作流、磁盘水位判定降级和录像压缩归档。本票须实际执行已经配置的录像滚动窗口，并负责该窗口缩短时必要的本机影响展示、确认和未来回收保护；不以这些后续任务代替基础录制和安全保留。
- 修改推理服务的感知计算路径、增加 NVIDIA 基座补丁、把主码流转用于预览，或经中心转发视频。
- 逐观看者 JWT/JWKS、企业身份源、公网视频分发、音频对讲、原生移动端。
- 固定海康型号、断言所有子码流都无 B 帧、承诺未经实测的并发路数；目标设备、GPU、网络、长稳和容量验证由关联票完成。
- 裁决 Q25 或 Q36，以及关闭实施票或勾选尚未验证的验收条件。

## Further Notes

### 原始验收与分期归属

下列原 AC 原文保持不变。每项的软件行为及上表必要证据属于 #34；右列只承载后置的完整跨 Module／真实环境证据。软件任务须完成实现、必要检查、独立审查及合并后才可关闭，不能仅因后置验证票存在而关闭，也不能把替身结果当作硬件通过。

| 原 AC | 原文 | #34 关单前的软件证据 | 后置验证 |
| --- | --- | --- | --- |
| AC1 | 每路相机可配置零转码或转码路径，浏览器使用推理机地址获取视频而非经中心中继。 | SYS-34-01／02／03／12／13／14 | #78、#82 |
| AC2 | mediamtx 按需拉流并生成可配置滚动窗口的分段录像；保留时长来自配置而非代码常量。 | SYS-34-04／05／06／07／11／14／17／18；按需与连续录像语义见 Implementation Decisions 8–9 | #78、#80 |
| AC3 | Web 支持多路状态、首帧失败和断流恢复提示，并满足键盘操作和非纯颜色表达。 | SYS-34-08／09／10／12／15 | #78、#82 |
| AC4 | 软件与自动测试不写死海康型号、B 帧结论或并发容量；这些结果仅进入发布验证证据。 | 配置及测试无硬件结论；SYS-34-03／16 | #77、#78 |

目前 #77、#78、#80、#82 均为开放验证票。依赖应由验证指向软件，不能反向阻塞软件实施。本次核对发现三处既有追溯缺口，实施关单前须定向补齐：#77 虽收录 AC4，但缺少对 #34 的原生依赖；#78 已原生依赖 #34，但正文漏收其关联的 AC4；#82 已原生依赖 #34，但正文漏收 AC1／AC3。相应验证票须保留缺失的原文、真实环境／浏览器验证入口和证据要求；本 spec 明确这些待办，不把现有链接写成追溯已经完整，也不代替验证结果。

### 决策依据与核对范围

- 仓库基线：`origin/main` 的 `5325952`，已核对 #34 正文及评论、#25 的关闭状态、原生依赖和相关验证票。本票沿用已有标签 `ready-for-agent`；生成 spec 不代表实现完成。
- 领域语言：[CONTEXT](https://github.com/cybest993-xx/nvsop/blob/main/CONTEXT.md)。规范依据：[方案 Q12／Q28、§八–九](https://github.com/cybest993-xx/nvsop/blob/main/docs/design/solution-and-roadmap.md)、[媒体与保留 §5.5／§5.19](https://github.com/cybest993-xx/nvsop/blob/main/docs/design/mechanisms/evidence-and-retention.md)、[控制面 §5.4／§5.15／§5.16](https://github.com/cybest993-xx/nvsop/blob/main/docs/design/mechanisms/control-plane.md)、[仓库 harness](https://github.com/cybest993-xx/nvsop/blob/main/docs/design/repository-harness.md)。
- 沿用 [ADR-0003 固定控制面前缀](https://github.com/cybest993-xx/nvsop/blob/main/docs/adr/0003-api-v1-is-a-fixed-prefix.md)、[ADR-0005 推理机自治](https://github.com/cybest993-xx/nvsop/blob/main/docs/adr/0005-judgment-runs-inside-the-inference-host.md)、[ADR-0007 基座复用与补丁范围](https://github.com/cybest993-xx/nvsop/blob/main/docs/adr/0007-base-is-the-trunk-not-a-dependency.md)、[ADR-0008 凭据只在推理机](https://github.com/cybest993-xx/nvsop/blob/main/docs/adr/0008-credentials-stay-on-the-inference-host.md)，不需要重开这些 ADR。
- MediaMTX 技术核对以官方 `v1.21.0` 的[配置定义](https://github.com/bluenviron/mediamtx/blob/v1.21.0/mediamtx.yml)、[源与 reader 生命周期](https://github.com/bluenviron/mediamtx/blob/v1.21.0/internal/core/path.go)、[回放查询](https://github.com/bluenviron/mediamtx/blob/v1.21.0/internal/playback/on_list.go)和[回放输出](https://github.com/bluenviron/mediamtx/blob/v1.21.0/internal/playback/on_get.go)为依据。Implementation Decisions 8–9 是结合这些行为与仓库连续录像要求得出的配置设计；实施需记录实际选定版本并以其公开 Interface 的测试确认，不依赖内部源码形状。
